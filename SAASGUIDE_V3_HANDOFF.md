# SaaSGuide V3 开发交接说明

## 1. 文档用途

本文件供新的 Codex 对话在 `D:\CodexProjects\SaaSGuide` 中读取。新对话负责按阶段推进 V3，首轮只实施阶段 1–5，完成后停止并交给用户验收。阶段 6–11 保留为后续路线，不得提前混入首轮范围。

执行前依次阅读：

1. 当前任务上下文中的 `AGENTS.md instructions`（由 Codex 客户端提供，不假定根目录存在同名文件）；
2. `README.md`；
3. `docs/PROJECT_STATUS.md`；
4. `docs/V2_ROADMAP.md`；
5. `docs/quality/KNOWN_LIMITATIONS.md`；
6. `docs/portfolio/EVIDENCE_INDEX.md`；
7. 本文件。

如文档与代码状态冲突，以当前代码、自动测试、浏览器结果和最新 Git 提交为证据，并把冲突写入阶段记录。

## 2. 当前基线

核对日期：2026-09-08。

| 项目 | 当前状态 |
|---|---|
| 项目目录 | `D:\CodexProjects\SaaSGuide` |
| 当前分支 | `v2-phase8-release` |
| 最新提交 | `4d8eed0 docs: add HR project explainer` |
| Git 远程 | 未配置 |
| 自动测试 | 135 项通过 |
| 数据与 JavaScript 检查 | 通过 |
| 产品形态 | 本地单用户项目风险应用 |
| 模型 | DeepSeek API |
| 公司数据 | 6 家虚构公司、30 个项目、180 条任务、48 个制度版本 |
| 固定规则基准 | TP=119、FP=4、FN=18、Precision=96.75%、Recall=86.86% |

现有实现包括数据接入、确定性风险扫描、版本化制度检索、DeepSeek ASK/PLAN、Python 输出校验、人工决策、SQLite 行动审计、CSV/XLSX 报告和固定规则评测。

当前数据和标准答案均为模拟内容。`company_id` 只提供本地上下文切换。DeepSeek 的真实浏览器证据为两次递进 ASK 和一次合法 PLAN；跨公司 30 项基准没有批量调用模型。

## 3. 用户目标和执行方式

用户希望把项目发展成可用于求职展示、本人能够解释、外部读者能够复现的 V3。重点是业务流程、AI 能力、证据和演示质量，不以页面数量或技术名词数量衡量完成度。

执行要求：

- 慢速推进，先核对现状再修改；
- 每个阶段单独形成规格、测试记录和 Git 提交；
- 首轮依次完成阶段 1–5，然后停止等待用户验收；
- 不配置远程仓库，不推送，不公开部署；
- 不使用真实公司、客户或个人敏感数据；
- 不改写历史验收结果；
- 模拟、自动测试、浏览器验证和真实 API 调用分开记录；
- UI 调整不得破坏已有 API、关键 DOM ID、人工确认、引用约束和审计链路；
- 必须由人完成的独立标注、账号授权、费用确认或真实文件操作，明确标记为外部依赖。

## 4. 文案风格

使用自然、直接、具体的中文。先说明产品在做什么，再在对应章节交代范围和限制。

- 避免“不是 A，也不是 B，它是 C”式模板开场；
- 减少连续的“不是……而是……”对比句；
- 避免“赋能、颠覆、智能化闭环、行业领先”等空泛词；
- 避免把多个技术名词堆成宣传口号；
- 用实际动作替代抽象表达，例如“校验引用编号”优于“保障可信 AI”；
- 说明限制时使用事实陈述，例如“当前没有账号权限和公网部署”；
- README 面向第一次进入仓库的人，测试记录负责保留技术细节。

推荐表达：

> SaaSGuide 把项目数据校验、风险规则、制度引用、DeepSeek 建议和人工确认放进同一条处理流程。当前版本在本地运行，使用模拟公司数据。

## 5. V3 产品方向

暂定名称：`SaaSGuide V3 — Evidence-Grounded Project Risk Copilot`，中文可称“有依据的项目风险助手”。名称只描述设计方向，不暗示真实企业采用或生产能力。

最终目标：

- 外部读者能在三分钟内理解项目；
- 用户本人能完整演示和解释关键取舍；
- 数据、规则、模型和人工决策形成连续业务链；
- AI 输出保留可核查的制度引用和运行记录；
- 评测区分规则、检索、模型和人工结果；
- 本地和公开演示都能安全使用模拟数据；
- 代码、测试、文档和浏览器表现一致。

## 6. 十一阶段路线

### 阶段 1：信息架构与仓库整理

目标：让第一次进入项目的人能找到当前版本，不被 V1 历史材料误导。

工作项：

- 清点根目录的当前文件、历史记录和交接材料；
- 设计 `docs/history/` 或等效归档结构；
- 移动文件前搜索全部引用，必要时保留说明页；
- 调整 README 第一屏，呈现问题、流程、AI 位置、截图和快速体验；
- 增加“V1 到 V3 的变化”和当前文件地图；
- 检查所有 Markdown 相对链接；
- 清理过时数字、本机绝对路径和冲突描述；
- 保留历史事实，不把旧测试数字改成当前数字。

完成闸门：

- 根目录只保留当前入口、运行文件、核心代码和必要配置；
- HR、产品和技术三条阅读路径均可从 README 到达；
- 链接检查通过；
- 新增 `docs/test_records/V3_PHASE1_INFORMATION_ARCHITECTURE_TEST_RECORD_日期.md`。

### 阶段 2：高完成度 UI 与演示体验

目标：统一八个主要页面的构图、层级、色调、状态和操作方式，让默认页面就能看出产品主线。

工作项：

- 先截图审计 `/`、`/data-sources`、`/risk-radar`、`/evidence-intake`、`/knowledge-base`、`/action-tracker`、`/reports`、`/input-lab`；
- 建立统一的设计令牌、页面标题区、主要操作区、数据区和状态反馈；
- 删除页面中不必要的开发说明，把解释移入帮助文档或折叠区；
- 改善空、加载、错误、成功和禁用状态；
- 强化“规则证据—AI 建议—人工确认”的视觉关系；
- 增加安全的演示重置入口；
- 检查键盘焦点、颜色对比度和小屏布局；
- 保持已有 API、关键 DOM ID、工作流状态和安全边界。

完成闸门：

- 桌面端和 390×844 逐页浏览器检查；
- 默认页面构图有明显提升，不能只改圆角、颜色和间距；
- 无页面级横向溢出，无控制台错误；
- 核心端到端路径仍能运行；
- 保存前后对照截图和 `V3_PHASE2_UI_TEST_RECORD`。

### 阶段 3：完整业务闭环

目标：把现有模块连接成“数据到行动再到报告”的连续链路。

```text
导入或维护项目
→ 校验数据与来源
→ 扫描候选风险
→ 查看规则证据
→ DeepSeek ASK / PLAN
→ 人工确认风险
→ PLAN 预填行动
→ 人工确认并保存行动
→ 状态跟踪与事件审计
→ 报告汇总
```

工作项：

- 统一公司、项目、任务、候选、人工决策、行动和报告之间的 ID；
- 候选风险保留来源任务、规则证据、制度引用和扫描批次；
- PLAN 可以预填行动表单，但不能自动保存；
- 风险确认与行动确认分成两个明确闸门；
- 行动状态变化写入追加式审计事件；
- 报告从实际确认记录和行动生成；
- 补充撤销、重开、关闭及非法状态转换测试；
- 数据库迁移必须兼容已有本地数据并提供备份步骤。

完成闸门：

- 至少一个模拟项目完成整条浏览器链路；
- 任一步缺少确认都不会形成正式行动；
- 每个报告数字可以回溯到记录；
- 自动测试、迁移测试和 `V3_PHASE3_WORKFLOW_TEST_RECORD` 通过。

### 阶段 4：独立评测框架

目标：把规则、检索、模型和人工判断分开评估，并减少同源标注偏差。

同一个对话生成场景再生成“标准答案”不构成独立评测。Codex 可以建立评测工具和盲标包，真正的独立标签需要用户或另一位未参与场景生成的人完成。

工作项：

- 写清风险标签、严重度、ASK/PLAN 和引用判断标准；
- 建立去除系统预测的盲标文件；
- 提供人工标注界面或简单模板；
- 支持两位标注者及分歧处理；
- 划分开发集和留出集，禁止在留出集上反复调规则；
- 分开计算规则 Precision、Recall、F1；
- 评估检索召回、引用支持度、模型结构合格率和行动可执行性；
- 建立误报、漏报、引用错误、信息不足和建议不可执行等错误分类；
- 记录数据、规则、Prompt 和模型版本。

完成闸门：

- 评测程序和盲标流程可运行；
- 没有第二位标注者时，状态只能写为“独立评测框架完成，外部标注待完成”；
- 不得把人工标注写成系统自动完成；
- 形成 `V3_PHASE4_EVALUATION_TEST_RECORD` 和标注说明。

### 阶段 5：DeepSeek 稳定性与可观察性

目标：让模型失败、超时、格式错误和费用都能被看见和控制。

工作项：

- 使用严格 JSON Schema 或等效数据模型校验 ASK/PLAN；
- 为 Prompt、输出协议和模型名称增加版本号；
- 增加超时、有限重试、退避和错误分类；
- 增加单次调用和批量调用预算上限；
- 记录去敏后的运行 ID、延迟、重试次数、Token 或可用费用字段；
- 相同输入可重复测试，但不得把随机模型结果写成固定承诺；
- 模型不可用时保留规则结果和人工路径；
- 区分本地预检查 ASK 与 DeepSeek ASK；
- 设计模型供应商接口，暂不为了展示而接入多个供应商；
- 批量真实调用需要可用密钥和明确费用范围。

完成闸门：

- 合法 ASK、合法 PLAN、超时、限流、非法 JSON、伪造引用、超预算和供应商不可用均有测试；
- 页面能说明当前模型状态和失败原因；
- 少量真实 API 验收与模拟错误测试分开记录；
- 形成 `V3_PHASE5_MODEL_RELIABILITY_TEST_RECORD`。

完成阶段 5 后立即停止。汇总阶段 1–5 的改动、测试、浏览器证据、未完事项和外部依赖，交给用户验收。未收到继续指令前，不进入阶段 6。

### 阶段 6：知识检索与文档理解

文档规模证明有需要后，再加入分块、关键词与向量混合检索、元数据过滤、重排序、引用定位、冲突检测和提示词注入防护。OCR、表格识别和语音转写分别立项和验收。

### 阶段 7：工程结构与自动化

拆分 `server.py`，使用 Blueprint 或等效模块组织 API；建立统一数据模型、数据库迁移、依赖锁定、类型检查、覆盖率、端到端测试、结构化日志和 Windows/Linux CI。

### 阶段 8：安全、权限与数据治理

在接触真实数据前完成身份认证、角色权限、真正的租户隔离、密钥管理、文件扫描、敏感信息识别、保留期限、数据删除、数据库保护、备份恢复、限流和威胁模型。

### 阶段 9：真实平台连接器

根据目标用户实际使用的平台选择一个只读连接器，验证授权、同步、增量更新、错误恢复和审计，再讨论人工确认后的写回。不要同时接入多个平台制造表面丰富度。

### 阶段 10：GitHub 作品集发布

创建私有仓库后推送，真实运行 GitHub Actions，扫描完整历史中的密钥和个人信息，在干净环境安装，制作 Release、截图和演示视频。公网演示只使用模拟数据，并在安全检查后决定是否开放。

### 阶段 11：真实业务验证

选择一个清晰子场景，访谈目标用户，使用经过授权和去标识化的数据开展小规模试用。记录现有流程基线、建议接受、修改和拒绝情况，再决定是否投入多人协作、通知和商业化。

## 7. 首轮实施范围：阶段 1–5

新对话应按以下顺序工作：

1. 检查 Git 状态和当前测试，必要时从 `4d8eed0` 创建 `codex/v3-development` 分支；
2. 为阶段 1 写简短规格，实施、测试、记录并提交；
3. 在阶段 1 干净基线上实施阶段 2；
4. 依次完成阶段 3、4、5；
5. 每阶段结束运行完整 `run_checks.ps1` 和 `git diff --check`，完成对应浏览器验收；
6. 每阶段使用独立提交，不把五个阶段压成一次提交；
7. 阶段 5 完成后生成 `docs/V3_PHASE1_TO_5_ACCEPTANCE_SUMMARY.md`；
8. 停止实施并请用户检验页面、流程和说明。

首轮不包含：

- 阶段 6–11；
- 公开仓库、远程推送或在线部署；
- 未经确认的批量 DeepSeek 费用；
- 真实企业数据；
- 为增加关键词而搭建多 Agent；
- 大规模重写全部技术栈。

## 8. 每阶段验收记录

每份阶段记录至少包含：

- 目标和非目标；
- 修改文件；
- 数据或数据库迁移；
- 自动测试命令和实际数量；
- 浏览器环境、路由、视口和操作步骤；
- 真实 API 是否调用；
- 通过项、失败项和未验证项；
- 截图或输出证据路径；
- 已知限制；
- Git 提交号。

不得提前填写未发生的测试数量、截图、API 结果或提交号。

## 9. 持续保护的约束

- 首页只统计真实本地确认记录，不混入固定 Seed；
- 用户字段映射允许自由输入原表头，非法列由服务端拦截；
- AI 输出只形成草稿；
- 引用必须来自当前允许的制度白名单；
- 公司上下文不能串用；
- 模拟数据、固定评测和真实模型调用分开；
- 密钥、用户导入、SQLite 运行库、日志和备份不进入 Git；
- 单 Orchestrator 架构保持到出现清晰的并行自治需求为止；
- 公开表达不得把模拟结果扩展成企业准确率、商业收益或生产能力。

## 10. 新对话启动提示词

可以把下面内容直接发给新的 Codex 对话：

> 请打开 `D:\CodexProjects\SaaSGuide\SAASGUIDE_V3_HANDOFF.md` 并完整阅读，同时读取其中列出的当前状态、路线图、限制和证据文件。以提交 `4d8eed0` 为 V3 起点，依次实施阶段 1–5。每个阶段单独写规格、运行完整测试、完成浏览器验收、保存测试记录并单独提交。阶段 4 不得把同一生成来源的标签称为独立评测；需要人工标注时明确列为外部依赖。阶段 5 的批量真实 DeepSeek 调用必须先确认密钥和费用范围。完成阶段 5 后生成阶段 1–5 验收汇总并停止，等待我检验。不要进入阶段 6–11，不配置远程仓库，不推送，不公开部署。文案使用自然、具体的中文，避免模板化对比句和营销式表达。

## 11. 最终判断标准

V3 的价值不取决于页面数量、公司数量或 Agent 数量。理想结果是一条可以重复演示、可以追溯证据、能够暴露失败、本人能够解释的风险处理流程。真实用户、授权数据和独立评测到位后，项目才进入产品试点阶段。
