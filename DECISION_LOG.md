# 关键决策记录

## 1. 数据与页面分离

- 决定：风险和导览内容放入独立 JSON，页面负责读取和展示。
- 原因：修改数据时不必重写页面结构，统计可以由当前列表统一计算。
- 代价：页面必须通过本地服务打开，不能直接双击 HTML 完整运行。

## 2. AI 只建议，不自动决策

- 决定：DeepSeek 可以建议等级、优先级和行动，但不能直接修改状态或保存。
- 原因：模型可能误解输入；风险责任仍应由项目人员承担。
- 验证：真实分析后，只有点击确认才写入新风险；状态由程序固定为待处理。

## 3. 把引导生成器降为后台实验

- 决定：普通员工主页面聚焦风险分析，引导生成器只保留在 `/builder`。
- 原因：员工通常带着风险问题进入系统，不应先使用一个与任务无关的配置工具。
- 代价：ASK / BUILD 学习证据仍保留，但不再作为产品主卖点。

## 4. 第一版使用 JSON，不上数据库

- 决定：确认后的新风险写入 `risk-data.json`，写前备份。
- 原因：个人 Demo 的目标是证明完整流程，数据库、登录和多用户会显著增加成本，却暂时不增加核心学习证据。
- 风险：不支持多人同时写入、权限管理和复杂查询，因此不能描述为生产系统。

## 5. 统计来自当前列表

- 决定：总数、高中低等级、待处理和已处理都由当前风险数组重新计算。
- 原因：固定数字会在新增、筛选或状态变化后失真。
- 验证：真实新增后总数 4 → 5，高风险 3 → 4，待处理 2 → 3。

## 6. V2 开发前建立受保护的 V1 基线

- 决定：先复验 V1，再建立本地 Git 基线；`generated/` 与含个人信息的 `resume_work/` 不进入版本库。
- 原因：当前仓库没有历史提交，直接升级无法可靠区分 V1 与 V2，也可能把运行产物或个人求职材料带入未来公开仓库。
- 验证：2026-09-04 的 38 项自动测试、桌面 Chrome 最小闭环和 390 × 844 响应式检查通过，详见 `docs/test_records/V1_BASELINE_TEST_RECORD_2026-09-04.md`。

## 7. V2-P1 使用独立数据源页面

- 决定：在 `/data-sources` 增加 XLSX 导入闭环，不重构或替换 V1 风险看板。
- 原因：先证明数据来源、字段映射、错误拦截和人工确认，再把统一数据交给后续风险规则；这样也能单独回归 V1。
- 代价：当前 V1 风险列表与 V2-P1 导入结果尚未联动，这是阶段边界，不是已完成能力。
- 验证：固定模拟样本、13 项导入测试、2 项 V1 请求上限回归测试与 Chrome 内置样本闭环通过，详见 `docs/test_records/V2_PHASE1_TEST_RECORD_2026-09-04.md`。

## 8. 统一阶段命名和文档冲突优先级

- 决定：新文档统一使用 `V1-Pn`、`V2-Pn`；根目录旧 `PHASE*_TEST_RECORD.md` 保留原名并在索引中标为 V1 历史记录。
- 原因：单独写“Phase 1”无法判断属于 V1 还是 V2，直接重命名旧文件又可能破坏历史引用。
- 冲突优先级：当前阶段规格 > V2 路线图 > 总交接说明；已经发生的结果以对应测试记录和当前代码为证。
- 变更规则：产品方向、阶段范围或证据口径变化时，必须先在本文件记录原因、备选方案、影响和验证结果，不能静默改写历史目标。

## 9. V2-P2 先使用确定性规则，不调用模型

- 决定：第一批扫描只依据截止日期、状态、完成度和直接依赖生成候选风险。
- 原因：每次命中都能回到原字段、源表行号和明确规则，便于定位误报与漏报；模型语义提取属于后续阶段。
- 代价：无法理解自由文本、工作量语境和间接依赖链，因此固定评测保留一个误报和一个漏报。
- 验证：固定样本生成 6 条候选，TP=5、FP=1、FN=1；Precision 与 Recall 均为 83.33%。

## 10. 候选、人工决策与正式风险分离

- 决定：确认、观察、驳回和误报只追加到 `generated/risk-decisions.jsonl`，不修改任务或 V1 `risk-data.json`。
- 原因：候选只是规则信号；在没有正式转换流程和数据库审计前，不能让按钮隐式改变业务事实。
- 代价：当前页面重扫后不会自动恢复每张卡的最新人工状态，跨版本关联也尚未实现。
- 验证：四种选择均在 Chrome 中保存审计记录，Git 检查确认 `risk-data.json` 未改变。

## 11. 未评审候选不自动算误报

- 决定：评测只把标准答案明确列为负例且被规则命中的候选计为 FP；未标注的新候选单列为 `unassessed_predicted_keys`。
- 原因：缺少人工标注不等于错误，直接算 FP 会在扩充规则时人为压低 Precision。
- 验证：新增回归测试证明未评审候选不会进入误报集合。
# V2-P3 使用确定性候选作为离线验收底座

- 日期：2026-09-04
- 决策：先完成可复现的 TXT/Markdown/DOCX 解析、来源定位、ASK 和人工核对；没有真实模型调用时不声称 AI 抽取通过。
- 原因：文档中可能包含提示词注入，且模型服务和密钥并非离线验收的可靠前提。
- 影响：P3 可以证明解析和人机边界，不能证明真实模型抽取质量。
# V2-P4 先验证引用和拒答而非接入向量服务

- 日期：2026-09-05
- 决策：使用确定性词元检索完成版本、引用、冲突和拒答闭环。
- 原因：本阶段最重要的可验证风险是旧版本误用、无依据回答和引用不一致；向量服务会增加不可复现依赖。
- 限制：这不是语义向量 RAG，也不能证明复杂文档检索质量。
