# SaaSGuide 新建风险闭环验收记录

## 目的

验证普通员工可以直接提交一条项目风险，让 DeepSeek 辅助分析，并在人工确认后保存到本地风险列表。该流程使用虚构 SaaS 和模拟数据。

## 输入

- 风险标题：测试环境证书即将过期
- 风险描述：测试环境证书将在两天后过期，可能导致接口联调中断。
- 影响范围：登录与接口测试
- 责任人：陈宁
- 截止日期：2026-09-05

## 处理流程

1. 页面收集已知事实，不预先要求用户判断风险等级。
2. DeepSeek 返回结构化的 ASK 或 PLAN。
3. ASK 只能显示追问，不能保存；PLAN 必须通过程序校验。
4. 用户点击“确认加入风险列表”后，服务器才生成风险编号和待处理状态。
5. 写入前校验完整风险列表，并把旧 `risk-data.json` 保存到 `generated/backups/`。

## 真实运行结果

- 首次真实调用因本地服务无法访问外部网络而返回可见错误，重启为可联网服务后重试成功。
- 调用来源：DeepSeek API。
- 模型：`deepseek-v4-flash`。
- 决策：PLAN。
- 建议等级：高风险。
- 优先级：立即处理。
- 行动建议：3 条，每条都有完成信号。
- 本次用量记录：1050 tokens。
- 用户确认前风险总数保持 4；确认后变为 5。
- 新风险状态为待处理，AI 没有自动标记完成。
- 刷新页面后新风险仍存在，证明已写入本地 JSON。
- 已生成修改前的风险数据备份。

## 自动与浏览器验证

- 35 项 Python 自动测试全部通过。
- JavaScript 语法检查通过。
- JSON 与页面引用校验通过。
- 项目文件未发现硬编码 DeepSeek 密钥。
- 桌面页面加载完成，新增风险、统计和筛选同步更新。
- 390 × 844 手机尺寸无横向溢出，新建风险弹窗和操作按钮位于可操作范围。
- 低风险筛选在当前 0 条数据下正确显示“0 条模拟风险”。
- 本轮交互未捕获页面运行错误，页面资源无 4xx/5xx 加载失败。

## 当前限制

- 数据是个人学习 Demo 的本地模拟数据，不是真实公司风险。
- 使用 JSON 文件，不支持多人同时编辑、权限控制或数据库查询。
- DeepSeek 只提供建议，结果质量仍依赖输入事实和人工判断。
- 新建风险会持久保存；详情页中的临时状态修改和临时采纳计划仍不持久。
- 本地 Flask 开发服务不是生产部署方案。
