# SaaSGuide 项目交接说明

## 项目目的

为偏技术的 AIGC / AI 应用岗位准备一个能展示、能测试、能说明边界的个人学习项目。用户代码能力较弱，采用项目驱动学习：AI 可协助生成代码，但用户必须能运行、修改小处、定位错误并解释主要流程。

## 业务场景

- 产品名：SaaSGuide。
- 场景：某虚构的 B2B 项目管理 SaaS 使用“项目风险看板”帮助内部项目人员识别和处理风险。
- 核心输入：当前风险描述、影响范围、责任人、截止日期、当前状态、已有计划和可选补充说明。
- 核心处理：信息不足时返回 ASK；信息充分时返回 PLAN，整理建议等级、优先级、判断依据、行动计划和完成信号。
- 核心输出：风险看板中的 AI 分析建议、经用户确认的本次应对计划、结构化分析记录和运行日志。
- 次要实验：原功能 Brief → ASK / BUILD 引导生成器保留为后台学习证据，不属于普通员工主流程。

## 真实性边界

这是个人学习 Demo，使用虚构 SaaS 和模拟数据。不得宣称已接入真实 SaaS、拥有真实客户、已生产部署、已获得用户采用或业务指标。未完成、未运行、未测试的功能不得写入简历。

## 第一版技术范围

- 前端：HTML、CSS、JavaScript。
- 后端：Python + Flask。
- 数据：JSON 文件，不使用数据库。
- AI：合法获得的模型 API，第二阶段再接入。
- 测试：普通程序校验、浏览器交互检查、错误与回归记录。
- 版本与作品集：Git、GitHub、README、演示视频和测试证据。

第一版不做登录、多用户、支付、数据库、Docker、真实 SaaS 代码库接入、生产部署或真实客户数据。

## 开发顺序

1. 创建一个前端静态原型：虚构的项目风险看板 + 四步交互引导。
2. 将引导步骤和页面元素分离为 JSON。
3. 使用 Python 编写字段、步骤顺序和页面元素校验。
4. 接入模型 API，实现 ASK / BUILD 结构化输出。
5. 连接模型判断、校验、HTML 生成和文件保存。
6. 完成正常、缺字段、步骤重复、元素不存在、非法 JSON、API 失败和输出权限测试。
7. 整理 README、决策记录、Bug 日志、演示视频和简历证据。

## 当前执行进度

静态风险看板、JSON 数据分离、Python 校验、DeepSeek 结构化调用和本地 Flask 连接已经完成。2026-09-03 根据实际用户价值调整产品主线：DeepSeek 直接分析用户选中的项目风险，返回 ASK 或 PLAN；用户确认后才把建议应用为本次应对计划。原 ASK / BUILD 引导生成器降为后台学习实验。当前没有数据库或生产部署。

前端需包含：

- 项目风险看板的模拟页面。
- 风险总数、高风险筛选、风险详情和标记已处理四个目标区域。
- 显示当前步骤的引导卡片。
- 上一步、下一步、完成和退出交互。
- 目标区域高亮和完成状态。
- 页面明确标注“个人学习 Demo，使用虚构 SaaS 和模拟数据，未接入真实产品”。

静态原型和 JSON 数据分离已经在 Chrome 中逐步验收：页面可打开、布局无溢出、四步可切换、高亮目标正确、返回逻辑正确、完成状态可见，桌面和 390 × 844 手机尺寸通过。

第四阶段仅使用 DeepSeek。明显缺字段时由本地预检查直接返回 ASK 以避免不必要费用；字段完整时才准备调用 DeepSeek，模型返回仍须经过现有 Python 校验。密钥只允许从环境变量读取，不能写入前端、JSON 或项目文件。

作为历史学习实验，第五阶段引导生成入口为 `http://127.0.0.1:4173/builder`。Flask 只监听本机地址；浏览器无法读取 API 密钥。2026-09-03 的真实网页验收中，完整 Brief 返回 BUILD 并保存 4 步结果，含糊 Brief 返回 ASK 且没有覆盖合格文件。该页面不再作为普通员工的核心功能。

产品调整后的核心入口恢复为 `http://127.0.0.1:4173/`。风险详情包含 AI 风险助手；真实 API 验收返回 3 条带完成信号的建议。点击采纳只更新本次页面应对计划，风险等级、状态和概览统计不会由 AI 自动修改。四步导览改为用户主动启动，不再默认遮挡主流程。

2026-09-03 已继续完成“新建风险”实用闭环：员工填写风险事实后，DeepSeek 返回 ASK 或 PLAN；只有经用户确认的 PLAN 才能追加到本地 `risk-data.json`。保存前执行完整数据校验并在 `generated/backups/` 备份旧文件。真实浏览器验收新增“测试环境证书即将过期”模拟风险，列表由 4 条变为 5 条，刷新后仍存在；AI 建议为高风险、立即处理和 3 条行动，状态由程序固定为待处理。该闭环仍是本地单人 Demo，不等于数据库、多用户协作或生产级风险系统。

同日完成第一版本地收尾：导出当前风险 CSV、关闭并重新打开风险详情、38 项自动异常与回归测试，以及 README、决策记录、Bug 记录、演示脚本和简历证据说明。GitHub 发布与本人演示不由代码自动完成，仍需使用项目所有者自己的账号和真实讲解。

## 求职证据要求

每个阶段必须保留：可运行文件、测试记录、真实 Bug 和修复证据、当前已知限制、用户自己的解释。简历只能转化已完成且可验证的内容。
