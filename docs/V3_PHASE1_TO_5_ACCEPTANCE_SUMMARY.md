# SaaSGuide V3 阶段 1–5 验收汇总

更新日期：2026-09-09。

## 结论

阶段 1–5 已按本地单用户、模拟数据的个人学习 Demo 范围实现并留下独立提交、自动测试和浏览器证据。阶段 4 完成的是独立评测框架，外部标注仍为 `0/2`；阶段 5 的真实 DeepSeek 请求因网络不可用而失败，没有新增成功 ASK/PLAN 证据。

## 阶段与提交

| 阶段 | 实现提交 | 验收记录提交 | 已验证结果 |
|---|---|---|---|
| 1 信息架构 | `b577452` | `d9c61c4` | 根目录归档、README 路径、78 个本地链接 |
| 2 高完成度 UI | `c45b798` | `dee2e26` | 8 路由桌面/390×844 共 16/16，32 张前后截图 |
| 3 完整业务闭环 | `45b1fb0` | `24aad05` | 模拟项目从扫描、双人工闸门、行动状态到报告 |
| 4 独立评测框架 | `34e9c90` | `78d4fc0` | 4 开发 + 4 留出盲标案例，外部标注 `0/2` |
| 5 模型稳定性 | `0fd3529` | `9e7020f` | 严格协议、重试、错误分类、预算、遥测和失败保底 |

V3 实际起点是 `3452905`，不是交接初稿中的 `4d8eed0`；`3452905` 本身新增了 V3 交接文档。

## 当前完整检查

- 156 项 Python 自动测试通过；
- JSON 和页面数据校验通过；
- 78 个 Markdown 本地链接通过；
- 全部前端 JavaScript 语法检查通过；
- `git diff --check` 通过。

测试数量是截至阶段 5 的当前结果；各阶段记录保留当时实际数量。

## 浏览器证据

- 阶段 2：`docs/test_records/v3_phase2_ui_2026-09-08/`；
- 阶段 3：`docs/test_records/v3_phase3_workflow_2026-09-08/`；
- 阶段 4：`docs/test_records/v3_phase4_evaluation_2026-09-08/`；
- 阶段 5：`docs/test_records/v3_phase5_model_2026-09-09/`。

浏览器交互使用 Codex 应用内 Chromium。Chrome 控制通道不可用，不能把这些记录改写为 Chrome 验收。

## 失败与未验证项

- 阶段 3 的一次、阶段 5 的两次真实 DeepSeek 用户级请求因网络连接失败，没有产生成功结果；
- 阶段 4 没有两位独立人工标注者，没有一致性、裁决或独立评测分数；
- JSONL 与 SQLite 仍没有跨文件原子事务；
- 没有真实企业数据、真实客户、公开部署或商业结果；
- 没有证明跨进程配额、生产负载、多人并发或其他浏览器兼容性。

## 持续保护的边界

确定性规则输出候选，DeepSeek 只生成草稿，引用限于当前白名单。风险确认和行动确认是两个独立人工闸门。首页只统计本地确认记录，不混入固定评测或模拟 Seed。项目仍是一个自管 Orchestrator，不是多 Agent 系统。
