# 飞书代码与文档收尾记录

验收日期：2026-09-16
范围：收口此前未提交的飞书读同步、人工确认写回、真实租户验收及相关文档，不扩展其他业务功能。

## 本次修复

- 写回表 ID 必须显式配置，不再回落到读取表；
- 写回 Base 与表 ID 同时等于读取源时返回 `writeback_source_table_forbidden`，不发出网络请求；
- 本地行动缺少 `task_id` 或 `policy_reference` 时明确跳过，不生成占位制度依据；
- 日期时间戳固定按飞书业务时区 UTC+8 转换，避免 Windows 本机与 UTC Linux runner 相差一天；
- `.env.example` 补充写回变量、默认关闭状态和独立表约束；
- 飞书排障脚本移除硬编码 wiki token，默认隐藏 app token、table ID、record ID、访问令牌和响应正文；只有显式 `--show-identifiers` 的本机交互场景才显示配置所需资源 ID；
- 当前状态、发布材料和限制说明统一为 217 项测试、真实租户演示链路已验证、真实参与者仍为 0；
- 历史阶段记录保留当时结论，并增加后续状态指引。

## 自动检查

命令：

```powershell
& '.\run_checks.ps1'
```

结果：

- 217 项 `unittest` 通过；
- 32 项覆盖率采样通过，关键模块语句覆盖率 398/447（89.04%）；
- 类型契约、Git 跟踪文件密钥扫描、数据校验通过；
- Markdown 本地链接、11 个 JavaScript 文件语法和 `git diff --check` 通过；
- 新增 5 项回归测试：缺独立行动表、读写同表拒绝、缺任务编号、缺制度依据、保留真实制度依据。

## 浏览器验收

环境：本机 `127.0.0.1:4173`，Codex 应用内 Chromium。

| 页面 | 验收结果 |
|---|---|
| `/data-sources` | 飞书卡片显示本机未配置凭据；同步按钮禁用；页面明确写回仅允许人工已确认行动、目标为独立行动表、默认关闭且不修改源表 |
| `/action-tracker` | 制度依据字段和“推送已确认行动到飞书”按钮可见；点击后显示写入、重复跳过、本地闸门跳过数量及“客户原表未被修改” |
| 控制台 | 无 error 或 warning |

本轮没有重新调用真实飞书 API。2026-09-13 的真实租户验收使用作者自建演示数据，记录了 5 条读取、日期归一化、5 条风险候选、人工闸门、独立行动表写回、幂等和源表未修改。该证据不代表企业生产数据、批量稳定性、多租户安全或商业价值。

## 仍未验证

- 飞书批量上限、并发、配额、限流与长期稳定性；
- 人员、含时间日期、公式等更多字段类型；
- 多租户隔离、生产权限收敛和真实 SLA；
- 真实业务参与者、企业采用和建议效果。

## 结论

本批飞书遗留代码和当前口径矛盾已收口。写回安全边界由文档承诺提升为代码拒绝，排障脚本默认不再暴露资源标识符，当前材料能区分真实租户演示、自动测试、浏览器验收和仍未完成的生产/业务验证。
