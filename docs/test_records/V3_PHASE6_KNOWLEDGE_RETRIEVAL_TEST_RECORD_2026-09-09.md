# V3 阶段 6 知识检索与文档理解测试记录

日期：2026-09-09  
实现提交：`01c6820 feat: add V3 hybrid grounded retrieval`

## 目标与非目标

阶段 6 为当前模拟制度增加分块、元数据筛选、关键词与本地稀疏向量混合排序、版本重排、精确位置引用、冲突拒答和可疑指令排除。没有接入语义 Embedding、向量数据库、真实 OCR、图片表格识别或语音转写。

## 数据规模与修改

模拟 Seed 共 6 个公司、48 个制度版本；当前公司上下文读取 8 个版本。修改文件包括检索服务、知识 API、知识库页面、阶段规格、5 项新测试和两张浏览器截图。没有数据库迁移，没有模型调用。

## 自动测试

`run_checks.ps1`：161 项 Python 测试通过；JSON/页面校验、78 个 Markdown 本地链接和全部 JavaScript 语法检查通过。`git diff --check` 通过。

新增测试覆盖：

- 分块原文与字符位置；
- 关键词和稀疏向量分数；
- 元数据筛选；
- 可疑提示词指令片段排除；
- 同一文档多个生效版本时拒答；
- 已替代版本不覆盖生效版本。

既有 10 个固定模拟问题仍保持检索命中、引用和拒答 100%。该结果只属于同源小样本，不是独立或真实业务评测。

## 浏览器验收

环境：Codex 应用内 Chromium，桌面视口；控制台 warning/error 为 0。

1. `/knowledge-base?phase6=1` 查询“跨租户访问迹象要怎么处理？”；
2. 页面显示 `hybrid-retrieval-v1`、关键词 + 本地稀疏向量、模型未调用；
3. 回答引用“租户数据隔离标准”v1.0，显示 chunk `nebula-digital-tenant-data@1.0#c1` 和正文字符 1–33；
4. 查询“公司今年真实营收是多少？”；
5. 页面显示“依据不足，已拒答”，引用为空。

截图：`docs/test_records/v3_phase6_retrieval_2026-09-09/hybrid-answer.jpg`、`grounded-refusal.jpg`。

## 未验证与限制

- 没有外部独立检索集或真实企业文档；
- 本地稀疏向量依赖词面，不证明语义召回；
- 注入模式是有限关键词列表，不是完整防御；
- 字符位置适用于当前规范化字符串，不是 PDF 坐标或 DOCX 页码；
- 复杂表格、扫描件 OCR、图片和语音仍是独立未完成能力。
