# V1 安全基线复验记录（2026-09-04）

## 目的

在开始 SaaSGuide V2 前，确认现有 V1 可恢复、可运行、可测试，并排除密钥、个人求职材料和本地运行产物进入版本库。

## 自动检查

- 项目 `.venv` 可用。
- `run_checks.ps1` 实际运行完成。
- 38 项 Python 自动测试通过。
- `guide-data.json`、`risk-data.json` 与 `index.html` 校验通过。
- `app.js` 与 `builder.js` JavaScript 语法检查通过。
- 未发现项目内的密钥文件；DeepSeek 密钥仍只允许从环境变量读取。

## Chrome 桌面复验

环境：Windows 11、Chrome、`http://127.0.0.1:4173/`。

- 页面加载完成，浏览器视口为 1536 × 674；页面 `scrollWidth` 与可用内容宽度均为 1521，没有页面级横向溢出。
- 页面明确显示“个人学习 Demo，使用虚构 SaaS 和模拟数据，未接入真实产品”。
- 当前模拟数据统计显示：风险总数 5、高风险 4、待处理 3、已处理 0。
- 高风险筛选后显示 4 条：通过。
- 关闭风险详情，再点击风险行重新打开：通过。
- 四步导览顺序与目标区域：通过。
- 未处理风险时“完成”按钮不可用：通过。
- 标记当前风险已处理后，待处理由 3 变为 2、已处理由 0 变为 1，“完成”按钮可用：通过。
- 完成导览后显示“引导已完成”：通过。
- 页面控制台警告和错误：0 条。

以上处理状态只存在于当前浏览器页面；复验结束后已刷新页面，恢复 JSON 中的初始模拟状态。

## Chrome 手机复验

临时视口：390 × 844。

- `window.innerWidth` 为 390。
- 文档可用宽度与 `scrollWidth` 均为 375（其余宽度由浏览器滚动条占用），没有页面级横向溢出。
- 风险筛选、风险表格横向滚动区域、风险详情和导览完成提示均可见。
- 测试结束后已清除临时视口覆盖并恢复桌面状态。

## Git 与隐私保护

- 检查时仓库位于 `main`，没有历史提交，全部项目文件为未跟踪状态。
- `.gitignore` 排除 `.venv/`、Python 缓存、`.env`、日志和整个 `generated/`。
- `resume_work/` 含个人简历 PDF、渲染图片和生成脚本，与产品源码无关，已整体排除，避免个人信息误入公开仓库。

## 证据边界

本记录证明上述自动检查和 Chrome 用例在 2026-09-04 的本机环境通过。它不证明生产部署、长期稳定性、多用户并发、真实企业使用、真实业务效果或其他未测试浏览器兼容性。
