# SaaSGuide V2 发布后产品化修订验收记录

## 验收结论

本次修订通过自动回归、应用内 Chromium 交互检查和 DOCX 全页渲染检查。旧 `/v2` 产品路由已退役；根入口不再读取 V1 固定五条风险；XLSX 映射允许自由输入原始列名，并由服务端重新校验。

## 自动测试

- 命令：`& '.\.venv\Scripts\python.exe' -m unittest discover -v`
- 结果：96 项通过，0 项失败，0 项错误。
- 新增覆盖：`/v2` 返回 404；根页不包含固定风险；空工作台返回零值且不会创建 SQLite；工作台只统计确认导入；开放映射输入；不存在列返回明确错误。
- `validate_data.py` 通过；工作台兼容导览目标已经更新到当前 DOM。
- `app.js`、`data-sources.js`、`builder.js`、`risk-radar.js`、`evidence-intake.js`、`knowledge-base.js`、`action-tracker.js`、`reports.js` 和 `input-lab.js` 通过 `node --check`。
- `git diff --check` 通过，仅出现 Windows 行尾提示，无空白错误。

## 浏览器验收

- 环境：Codex 应用内 Chromium，本地服务 `http://127.0.0.1:4173`，当前浏览器表面约 872 CSS 像素宽。
- 根工作台读取到本机现有已保存状态：1 个确认导入、1 个项目、6 条任务、5 条风险人工决策、1 条证据核对、1 条进行中行动、0 条逾期行动。
- 根页没有固定 V1 风险标题；最近活动来自统一 JSON、人工审计 JSONL 和已存在的 SQLite 行动。
- 根页与数据源页均无页面级横向溢出；两页控制台日志均为空。
- 数据源有效样本自动建议 14 个字段，映射区为 14 个 `input type=text`、1 个 `datalist`、0 个映射 `select`。
- 在负责人映射中输入“我自己输入的负责人列”后，页面保留该自由输入，服务端返回“负责人映射的原始列不存在”与 `mapping_unknown_column`，并禁用确认导入。
- 恢复“负责人”并重新校验后显示“校验通过”，确认按钮恢复可用。
- `/v2` 退役由 Flask 接口测试确认 404。应用内浏览器对该导航返回客户端拦截，因此没有把浏览器错误页截图作为额外证据。

## 未完成的本轮验证

- 本轮应用内浏览器不提供精确 viewport 切换，未重新取得 390 × 844 的实时截图。CSS 含 980、700、420 像素断点且代码检查通过，但这不等于精确手机浏览器验收。
- 浏览器自动化仍不能代替用户选择本地文件。用户此前已确认两份文件都能选择并生成预览；本轮使用内置有效样本验证映射与错误恢复，上传和确认由接口测试覆盖。

## 文档验收

- 独立生成 `docs/SaaSGuide_V2_HR_演示引导.docx`，共 8 页。
- 使用 LibreOffice 和 Poppler 渲染全部 8 页并逐页检查。
- 修复第一轮标题下方蓝线与跨页编号延续问题后重新渲染；最终无裁切、重叠、缺字或表格溢出。

## 真实性边界

96 项测试和上述浏览器结果只证明当前本地代码在给定条件下通过。它们不证明真实企业文件兼容性、真实模型抽取、向量 RAG、OCR、语音识别、外部连接器、多人权限、生产稳定性或业务价值。
