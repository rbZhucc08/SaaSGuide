# V2-P2 确定性风险扫描验收记录（2026-09-04）

## 范围

本记录只覆盖统一项目 JSON 的确定性风险扫描、候选去重、固定评测和人工选择审计。没有自由文本/DOCX 提取、LLM 判断、RAG、数据库、语音、OCR、外部系统写回或生产部署。

## 自动检查

- `run_checks.ps1` 实际运行通过。
- 累计 70 项 Python 自动测试通过；V2-P2 新增 17 项。
- 新增测试覆盖：稳定候选与指标、重叠规则去重、源行证据、最终状态排除、三天边界、非法结构、非法日期、重复任务、四种人工选择、非法选择、超长说明、候选来源不一致、未评审候选、固定样本接口、最近导入接口、缺少导入和审计落盘。
- `risk-radar.js` 语法检查通过；V1 与 V2-P1 原测试无回归失败。

## 固定评测结果

固定扫描日：2026-09-12。模拟项目：星云 CRM 升级项目。

- 生成 6 条候选：4 条高风险信号、2 条中风险信号。
- 共 8 次规则命中；T-002 与 T-003 的两个逾期规则分别合并到单一 `schedule_delay` 候选。
- TP=5、FP=1、FN=1。
- Precision=83.33%，Recall=83.33%，False Positive Rate=100%。FPR 分母只有固定答案中的 1 个负例，因此不能单独外推。
- 固定误报：T-005 临近截止且低进度；规则没有考虑剩余工作量较小。
- 固定漏报：T-006 受到间接依赖链影响；第一批规则只检查直接阻塞依赖。
- 未被标准答案标注的新候选单列为未评审，不自动算误报。

## Chrome 桌面验收

环境：Windows 11、Chrome、`http://127.0.0.1:4173/risk-radar`。

- 页面明确显示“候选不等于事实”和“确定性规则，不调用模型”。
- 固定样本显示 6 条候选、8 次规则命中、Precision/Recall 83.33%、TP/FP/FN 5/1/1。
- 候选显示任务、负责人、风险类型、触发规则、原始字段值和源表行号。
- 真实点击“确认、观察、驳回、标记误报”均成功写入 `generated/risk-decisions.jsonl`。
- 页面每次提示选择只写审计记录，没有修改任务或 V1 风险；`risk-data.json` 的 Git 差异为空。

## V2-P1 → V2-P2 联动

- 扫描最近确认的 `valid_project_tasks_cn.xlsx` 标准化 JSON，扫描日设为 2026-09-12。
- 页面显示来源为“最近本地导入”，生成 4 条候选：2 条高风险信号、2 条中风险信号、6 次规则命中。
- 非固定样本不显示 Precision/Recall，避免没有标准答案时生成伪指标。

## 响应式与控制台

- 最近导入的 4 条候选在 390 × 844 视口下保留。
- `innerWidth=390`、`innerHeight=844`、`scrollWidth=390`、`clientWidth=390`，没有页面级横向溢出。
- 第一张候选卡宽度为 314 像素；导航允许自身横向滚动。
- 浏览器控制台警告和错误：0 条。
- 测试后已清除临时设备尺寸覆盖。

## 结论与边界

V2-P2 规格中的确定性扫描闭环已实现并验收。83.33% 只适用于 1 份人工编写的模拟评测集；样本量小、负例只有 1 个，不能声称真实准确率、企业效果或完整风险发现能力。
