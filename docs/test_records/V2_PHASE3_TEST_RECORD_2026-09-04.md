# V2 Phase 3 验收记录

日期：2026-09-04

## 实现结果

- TXT、Markdown、DOCX 解析、来源哈希、段落/表格位置和原文片段已实现。
- 固定 DOCX 得到 3 条明确任务事实、2 条待确认信息、1 条可疑指令文本。
- 人工接受、修改、驳回记录只写本地 JSONL 审计，不修改任务或正式风险。
- 项目虚拟环境实际加载 `python-docx 1.2.0`。联网安装两次失败，最终从 Codex bundled Python 复制同版本包及依赖到项目虚拟环境；此复制属于本机环境恢复，不是可分发安装验证。

## 自动测试

- 74 项累计测试通过，其中 P3 新增 4 项。
- `evidence-intake.js`、既有 JavaScript 入口语法检查通过。
- 覆盖 DOCX 固定答案、UTF-8/GBK、格式拒绝和人工审计不写回。

## 浏览器验收

- Chrome 桌面：页面可打开；固定样本显示 16 个片段、6 个候选、3 个明确事实、2 个待确认和 1 个可疑指令；可疑指令默认驳回。
- Chrome 桌面截图目视检查：候选卡片、段落位置、默认选择和保存区域无重叠或裁切。
- Chrome 390 x 844：本轮浏览器控制接口不能改变真实视口，未验证；CSS 移动断点不等于浏览器验收。
- 控制台：本轮浏览器控制接口未提供控制台读取能力，未验证；页面交互成功且无可见运行时错误不等于控制台零错误。
- 真实文件控件：未由自动化设置；后端上传由自动测试覆盖，用户此前只验证过 XLSX 文件控件，不应外推为 DOCX 上传验证。

## 未验证和限制

- 没有调用 DeepSeek 或其他模型，因此“AI 结构化提取”未验收；本阶段完成的是可离线复现的解析和候选生成底座。
- 未测试含图片、文本框、批注、修订、加密或宏的 DOCX。
- 未验证 Edge、Safari、macOS、Linux、并发上传和生产负载。
