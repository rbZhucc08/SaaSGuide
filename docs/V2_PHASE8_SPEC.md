# V2 Phase 8 本地发布与作品集收尾规格

> 历史说明：本规格记录 P8 当时的验收目标。2026-09-05 产品化修订已删除 `/v2` 总览，当前入口改为 `/`，详情见 `CHANGE_RECORD_2026-09-05_PRODUCTIZATION.md`。

## 目标

提供可复现的本地 V2 演示入口、健康检查、安全响应头、轻量错误记录、架构与演示说明，并完成最终回归。

## 验收

- `/v2` 链接 P1-P7 的全部核心页面并显示已实现、浏览器验证和未验证边界。
- `/health` 返回服务版本与 `simulated-data-only` 范围。
- HTML 和 API 均有安全响应头。
- 启动脚本先检查运行依赖，再只在 localhost 启动。
- 全量自动测试、JavaScript 语法、浏览器桌面/390 x 844、控制台、Git 状态和敏感值检查留证。

## 公开部署闸门

没有用户指定的部署平台、账号权限和真实访问测试，不创建或声称公开 URL。当前完成的是本地可演示版本。
