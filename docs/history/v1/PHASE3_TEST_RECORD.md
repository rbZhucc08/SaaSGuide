# SaaSGuide 第三阶段 Python 校验记录

## 阶段目标

在 JSON 数据进入页面或后续 AI 流程前，自动发现结构和映射错误。

## 已完成

- 新增 `validate_data.py`，只使用 Python 自带功能，不依赖第三方包。
- 校验 `guide-data.json`、`risk-data.json` 和 `index.html`。
- 新增 `test_validator.py`，自动验证正确数据和五类错误数据。

## 校验范围

- JSON 文件是否存在、格式是否合法。
- 顶层对象和列表是否存在。
- 引导步骤是否包含编号、目标、标题和说明。
- 步骤编号是否严格按照 1、2、3、4 排列。
- 引导目标是否重复、是否在页面元素表中定义。
- 页面元素指向的 HTML id 是否真实存在。
- 风险字段和字段类型是否正确。
- 风险编号是否重复。
- 风险等级、中文等级名称和状态是否在允许范围内。
- 日期是否为有效的 YYYY-MM-DD。
- 应对计划是否为非空文字列表。
- HTML 是否存在重复 id。

## 自动测试结果

- 当前真实项目数据通过校验。
- 缺少引导标题：成功拦截。
- 重复步骤编号：成功拦截。
- 页面目标不存在：成功拦截。
- 风险状态非法：成功拦截。
- 风险编号重复：成功拦截。
- 总计 6 项测试，全部通过。

## 测试中发现的真实问题

- 初次运行时，校验器把第一条风险的空 `avatarClass` 判为错误。
- 实际含义：第一条风险使用默认头像颜色，不需要额外样式名称，空值是合法的。
- 修复：将 `avatarClass` 定义为可选样式；缺失或空字符串允许，非文字类型仍会拦截。
- 修复后：真实数据校验和全部 6 项自动测试通过。

## 关键流程

```text
读取 JSON 和 HTML
        ↓
检查字段、类型、顺序和重复项
        ↓
检查引导目标是否存在于页面
        ↓
无错误：返回通过和退出码 0
有错误：列出问题并返回退出码 1
```

## 当前限制

- 校验器只能检查已经定义的规则，不能保证业务内容一定合理。
- 当前允许的风险等级、状态和引导动作是项目内固定集合。
- 非法 JSON、文件缺失和更多组合错误将在完整异常测试阶段继续补充证据。
- 当前需要主动运行 `validate_data.py`；它能报告失败并返回退出码 1，但尚未自动阻止本地网页启动。强制形成“校验通过后才展示”的关卡属于后续完整流程连接阶段。
- 尚未连接模型 API；目前校验的是本地模拟数据。
