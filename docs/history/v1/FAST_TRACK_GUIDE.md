# SaaSGuide 快速掌握与求职交付指南

## 1. 目标

用最短路径完成一个可运行、可测试、可解释的 AI 应用学习 Demo，并形成可用于求职的项目证据。

本项目使用虚构 SaaS 场景和模拟数据。它不是客户项目，没有接入真实产品，也不能证明真实用户采用或业务增长。

## 2. 加速协作方式

后续默认由 AI 连续完成实现、检查和记录，用户不重复机械操作。每个阶段只学习下面六项：

1. 目的：这一步解决什么问题。
2. 输入：它从哪里获得信息。
3. 处理：信息经过什么判断或转换。
4. 输出：最后产生什么结果。
5. 验证：怎样证明结果正确。
6. 限制：目前还不能做到什么。

Prompt 不是单独的学习目标，只是表达需求的工具。更重要的是数据口径、结果判断、测试证据和真实性边界。

## 3. 用户不需要重复的内容

- 从头手写整页 HTML、CSS 或 JavaScript。
- 反复输入格式相似的练习 Prompt。
- 为了记忆而重复同一套点击步骤。
- 背诵语法、命令或文件内容。
- 手工执行可以由程序完成的机械检查。

## 4. 用户必须保留的最低参与

为了能把项目写进简历，项目完成前用户必须亲自完成一次最终演示，并能回答：

- 这个项目解决什么问题，目标用户是谁？
- 输入、处理和输出分别是什么？
- ASK 和 BUILD 为什么需要区分？
- 数据从哪里来，怎样检查数据与页面一致？
- 一个真实发现的问题是什么，怎样修复并回归检查？
- 哪些部分已经运行和测试，哪些仍是计划？
- 为什么它只能称为学习 Demo，不能称为真实 SaaS 项目？

这不是代码考试。用户可以查看自己的项目说明，但必须能独立操作和解释主要流程。

## 5. 完整项目路线

### 阶段一：静态交互原型

- 目的：先看见最终产品的大致形态，验证风险看板和四步引导是否合理。
- 输入：写在页面中的模拟风险和引导步骤。
- 处理：JavaScript 负责筛选、切换步骤、高亮区域和更新处理状态。
- 输出：可点击的风险看板网页。
- 验证：检查桌面和手机布局、四步顺序、筛选、详情、处理、完成和重新开始。
- 限制：数据固定在本地文件中，刷新后不保存，没有真实 SaaS 数据。

用户需要知道：`index.html` 放页面内容，`styles.css` 决定外观，`app.js` 决定点击后的变化，`PROJECT_BRIEF.md` 规定范围和验收标准。

### 阶段二：分离结构化数据

- 目的：让内容与页面代码分开，避免每次改数据都修改网页结构。
- 输入：风险信息和引导步骤。
- 处理：按照统一字段整理为 JSON。
- 输出：程序可以读取的结构化数据文件。
- 验证：增加、删除或修改一条数据后，页面结果与统计同步变化。
- 限制：仍是本地模拟数据，不是数据库或真实产品数据。

用户需要知道：JSON 是统一格式的数据清单；字段名称、必填项、编号和数据类型必须稳定。

### 阶段三：Python 自动校验

- 目的：在错误内容进入页面前发现问题。
- 输入：结构化引导数据和页面目标区域清单。
- 处理：检查缺少字段、步骤重复、顺序错误、目标区域不存在和格式非法。
- 输出：通过结果或可理解的错误报告。
- 验证：分别提供正确与错误样例，确认程序能够放行正确数据并拦截错误数据。
- 限制：校验程序只能检查已定义的规则，不能自动判断所有业务内容是否合理。

用户需要知道：Validation（校验）不是生成内容，而是阻止不合格结果继续流转。

### 阶段四：DeepSeek 模型 ASK / BUILD

- 目的：信息不足时先追问，信息充分时才生成引导。
- 输入：功能 Brief、目标用户、入口、步骤、成功状态和页面元素。
- 处理：模型按照规则返回 ASK 或 BUILD，并采用固定结构输出。
- 输出：追问清单或完整引导方案。
- 验证：使用完整需求、缺字段需求和含糊需求分别测试。
- 限制：模型可能理解错误，必须经过程序校验；API 需要合法密钥并可能产生费用。

用户需要知道：ASK 是停止生成并补充信息，BUILD 是信息足够后生成；Structured Output（结构化输出）让程序能够继续处理模型结果。

当前实现补充：只支持 DeepSeek。程序先免费检查明显缺字段；字段齐全时才调用模型。密钥从 `DEEPSEEK_API_KEY` 环境变量读取，不进入网页或项目文件。模拟响应测试已通过；2026-09-03 又完成两次真实调用，完整 Brief 返回 BUILD，语义含糊 Brief 返回 ASK，两者均通过结构校验。

### 阶段五：连接完整流程

- 目的：把需求、模型判断、校验和页面输出串成一条可运行链路。
- 输入：用户提交的功能 Brief。
- 处理：接收需求，调用 ASK / BUILD，校验结果，保存合格内容并交给页面展示。
- 输出：可查看的引导结果和运行记录。
- 验证：跟踪一次请求从输入到输出的完整路径，并确认错误发生时能够停止。
- 限制：这是本地学习应用，不做登录、多用户、数据库、支付、Docker 或生产部署。

用户需要知道：每一部分只承担一种职责，上一部分的输出会成为下一部分的输入。

当前实现补充：`server.py` 提供本地 Flask 服务，`builder.html` 收集 Brief，`builder.js` 显示 ASK / BUILD。只有通过校验的 BUILD 才写入 `generated/guide-data.generated.json` 和 HTML 预览；ASK 只显示问题并记录一次运行。密钥不会发送给浏览器。

### 阶段六：异常测试和回归检查

- 目的：证明项目不只在理想输入下运行。
- 输入：正常、缺字段、重复步骤、元素不存在、非法 JSON、API 失败和保存失败等情况。
- 处理：记录预期结果、实际结果、问题原因和修复内容。
- 输出：测试记录、Bug 日志和修复证据。
- 验证：修复后重新运行旧测试，确认没有破坏已有功能。
- 限制：测试覆盖已列出的场景，不代表生产级可靠性或安全认证。

用户需要知道：Regression Test（回归测试）就是修复一个问题后，再检查以前正常的功能有没有被破坏。

### 阶段七：作品集和简历证据

- 目的：把技术文件转化为招聘方能够核实的项目证据。
- 输入：可运行文件、测试结果、Bug 记录、决策记录和演示过程。
- 处理：整理 README、Git 记录、演示视频和简历表述。
- 输出：可展示、可解释、不过度包装的求职项目。
- 验证：用户亲自完成一次演示，并回答项目流程、问题、修复和限制。
- 限制：只能描述已经完成、运行和验证的内容。

## 6. 当前实际进度

### 产品方向修正

原 AI 引导生成器展示了 ASK / BUILD、结构化输出和校验，但它主要面向产品配置人员，不能直接证明“帮助员工管理风险”。现在主线改为 AI 风险助手：员工直接查看风险，按需让 DeepSeek 分析选中风险，并在人工确认后采纳行动计划。引导生成器保留为后台实验，不再占据主页面入口。

- 已完成：第一阶段静态网页、四步引导、筛选、风险详情、处理状态和重新开始逻辑。
- 已完成：JavaScript 语法检查和主要页面区域检查。
- 已完成：此前对桌面和手机尺寸进行过 Chrome 交互验收，并修复遮挡与溢出问题。
- 最近修改：四张概览卡改为只统计当前风险列表；总数、风险等级、待处理和已处理由页面统一计算。
- 第二阶段已实现：四步引导和页面目标已移入 `guide-data.json`；风险列表、详情、应对计划和初始状态已移入 `risk-data.json`，页面通过 `app.js` 统一读取和生成。
- 第二阶段已检查：两个 JSON 的格式、四步顺序、目标映射、风险数量和本地预览读取均通过。
- 已完成：最近统计修改及 JSON 分离后的 Chrome 桌面与 390 × 844 手机尺寸回归检查，未发现页面错误。
- 第三阶段已实现：Python 校验器检查 JSON 格式、必填字段、步骤顺序、重复编号、风险状态和页面目标；当前项目校验与 6 项自动测试通过。
- 第四阶段已实现：DeepSeek 请求、ASK / BUILD 固定结构、环境变量密钥读取、模型输出校验和错误处理；8 项新增离线测试通过。
- 已验证：DeepSeek API 真实认证与调用、真实 BUILD 分支和真实 ASK 分支。
- 尚未验证：长期输出稳定性、并发请求、精确 token 成本记录，以及余额不足、限流和服务异常的真实触发过程。
- 第五阶段已完成：本地 Flask 串联、网页 Brief 输入、真实 ASK / BUILD 显示、校验、JSON/HTML 保存和运行日志；21 项自动测试通过，桌面与 390 × 844 验收通过。
- 实用方向调整已完成：主页面接入 AI 风险助手，真实 PLAN 分析和人工采纳流程通过；四步导览改为可选启动；累计 31 项自动测试通过。
- 新建风险闭环已完成：填写事实 → DeepSeek ASK/PLAN → 人工确认 → 校验 → 备份旧文件 → 写入本地 `risk-data.json`。真实新增 1 条模拟风险后总数由 4 变为 5，刷新后仍存在；累计 35 项自动测试通过，桌面与 390 × 844 手机尺寸验收通过。
- 第六阶段已完成：38 项自动测试覆盖主要正常、缺字段、非法输出、API 错误、超大请求和保存失败场景；完整记录见 `PHASE6_TEST_RECORD.md`。
- 第七阶段本地材料已完成：README、决策记录、Bug 记录、3 分钟演示稿和简历证据边界已经整理；GitHub 发布和用户本人演示仍需用户账号与本人完成。
- 当前限制：已确认的新风险可以在本地 JSON 中保留，但详情页临时修改的处理状态和 AI 计划仍会在刷新后恢复；没有登录、多用户冲突处理、数据库或生产部署。
- 运行方式变化：独立 JSON 需要通过本地预览地址读取，不能再直接双击 `index.html` 使用完整页面。

## 7. 最快但可信的交付标准

项目进入简历前至少需要：

- 一次最终端到端运行。
- 一份测试与 Bug 记录。
- 一份清楚说明输入、处理、输出和限制的 README。
- 一段用户能够亲自讲明白的项目介绍。
- 一条不夸大结果的简历项目描述。

如果缺少最终运行和解释，只能写“参与需求设计或 AI 辅助原型探索”，不能写“完成并测试了该项目”。

## 8. 后续每阶段的汇报格式

AI 完成一段工作后只汇报：

```text
完成了什么：
关键流程：
你只需记住：
验证证据：
当前限制：
下一步：
```

除最终演示和必要决策外，不再要求用户重复机械步骤。
