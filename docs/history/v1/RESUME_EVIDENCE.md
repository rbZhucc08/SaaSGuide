# 求职证据与表述边界

## 可以核实的事实

- 独立完成一个本地可运行的 AI 风险管理学习 Demo。
- 使用原生 HTML/CSS/JavaScript 构建风险看板和响应式交互。
- 使用 Python、Flask 和 JSON 串联风险输入、DeepSeek ASK/PLAN、结构校验、人工确认、备份和持久化。
- 设计模型输出约束和失败拦截，避免 ASK、非法结构或未确认建议直接写入风险列表。
- V1 基线为 38 项自动测试；V2 发布后修订的最终测试数与浏览器结果见对应验收记录。
- 将模拟项目任务 XLSX 统一为可追踪 JSON，并以 Python 确定性规则生成带源行证据的候选风险，支持去重、固定评测和人工误报反馈。
- 为 V2 增加单 Orchestrator 受控编排：确定性候选与版本知识检索先行，DeepSeek 输出 ASK/带引用 PLAN，Python 阻断伪造引用、缺失字段和超过 3 条的行动草稿。
- 真实浏览器 API 验收取得两次递进 ASK 与一次 PLAN；运行记录保留模型状态和三段 Skill trace，不保存密钥。
- 固定模拟评测集 Precision 与 Recall 均为 83.33%，不代表真实业务准确率。
- 记录真实故障、修复过程、产品方向取舍和当前限制。

## 简历项目描述初稿

**SaaSGuide｜AI 项目风险助手（个人学习 Demo）**

- 基于 HTML/CSS/JavaScript 与 Flask 搭建本地 V2 工作台，聚合用户确认导入、人工风险决策、证据核对和 SQLite 行动状态；无用户数据时显示真实空状态。
- 接入 DeepSeek 结构化 ASK/PLAN 工作流：信息不足时追问，信息充分时输出建议等级、优先级、判断依据、行动和完成信号，并通过 Python 校验阻止异常输出继续流转。
- 将 V2 风险研判拆成一个受控 Orchestrator 和五个领域 Skill 合约；当前调用实际串联风险扫描、证据检索与行动规划，并强制引用白名单和人工确认。
- 设计“AI 建议—人工确认—备份—JSON 持久化”机制，验证新增风险在刷新后保留，同时不允许模型自动修改风险状态。
- 编写覆盖非法 JSON、缺字段、错误步骤、API 异常、超大请求、保存失败、XLSX 导入、确定性规则、评测、人工决策和产品入口的自动测试；具体通过数以最新验收记录为准。
- 扩展 XLSX 数据接入与确定性风险扫描：保留来源哈希和源行证据，按项目、任务与风险类型去重，并以固定模拟标准答案计算查准率/查全率；人工选择只写审计记录。

## 面试时不能说

- “帮助某公司降低了项目风险”——没有真实公司与效果数据。
- “已上线并服务真实用户”——只在本机 Flask 开发服务运行。
- “实现企业级 Agent/RAG/数据库系统”——当前只有本地单 Orchestrator、确定性检索和 SQLite；没有向量 RAG、多 Agent、多用户或生产运行。
- “AI 自动判断和处理风险”——AI 只建议，最终确认由用户完成。
- “具备大模型训练经验”——本项目是模型 API 应用，不涉及训练或微调。

## 发布前仍需用户完成

- 用自己的 GitHub 账号创建仓库并发布本地代码。
- 亲自录制一次 3 分钟演示，确认能够解释输入、处理、输出、验证和限制。
- 在简历中保留“个人学习 Demo”和“模拟数据”边界。
# V2-P3 可用证据

- 已实现 TXT、Markdown 与 DOCX 的离线解析，保留文件哈希、原文和段落/表格位置。
- 已在固定虚构周报上验证 3 条明确任务事实、2 条待确认信息、1 条提示词注入文本，并保存人工核对审计。
- 不能表述为真实企业文档处理、生产 OCR 或真实大模型抽取经验。
# V2-P5 可用证据

- 已设计并实现 SQLite 版本迁移、关联实体、人工确认行动、受限状态机和追加式事件审计。
- 已用固定模拟候选验证创建、逾期、进行中和完成流程。
- 不能表述为企业级数据库、多租户权限或生产并发经验。
# V2-P6 可用证据

- 已用 Python 计算风险、误报、逾期和平均处理时间，并生成 CSV 与包含原生趋势图的三表 XLSX。
- 已检查 XLSX 关键范围、公式错误并渲染全部工作表。
- 只能表述为固定模拟数据的报告闭环，不能表述为真实运营成果。
# V2-P7 可用证据

- 已实现普通 PDF 文本提取、扫描件检测、WAV 元数据校验和只允许模拟模式的适配器合约。
- 已用 Poppler 逐页检查自建 PDF 样本。
- 不得写“已完成 OCR、语音识别或真实 Jira/飞书集成”。
