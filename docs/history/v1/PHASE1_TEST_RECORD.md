# SaaSGuide 第一阶段测试与修复记录

## 项目性质

个人学习 Demo，使用虚构 SaaS 和模拟数据，未接入真实产品、真实客户、数据库或模型 API。

## 当前测试结论

### 已确认

- `app.js` JavaScript 语法检查通过。
- 页面包含四个引导目标：风险总数、高风险筛选、风险详情、标记已处理。
- 第一轮 Chrome 桌面尺寸 1440 × 900 交互验收通过。
- 第一轮 Chrome 手机尺寸 390 × 844 交互验收通过。
- 第一轮验收时页面没有发现运行错误。
- 第一阶段验收当时只有前端和说明文件，没有后端、数据库或模型 API 调用；JSON 文件在后续第二阶段按计划加入。

### 最近修改后的程序检查

- 风险列表共有 4 条模拟风险。
- 风险等级为高风险 3 条、中风险 1 条。
- 初始状态为待处理 2 条、处理中 2 条、已处理 0 条。
- 概览数字改为从当前风险列表统一统计。
- 高风险占比改为根据高风险数量与风险总数计算。
- 点击处理后会重新统计待处理和已处理数量。
- 重新开始引导时会恢复初始状态并重新统计。
- 最近修改后的 JavaScript 语法检查通过。

### 后续回归确认

- 统计逻辑和 JSON 数据分离完成后，已进行 Chrome 桌面与 390 × 844 手机尺寸回归检查。
- 初始概览显示 4、3、2、0，高风险占比为 75%：通过。
- 处理“待处理”风险后显示 4、3、1、1：通过。
- 处理“处理中”风险后，待处理保持 2、已处理变为 1：通过。
- 重新开始后恢复 4、3、2、0：通过。
- 页面错误检查：未发现错误。

## 已发现并修复的问题

### 1. 未处理风险也能完成引导

- 现象：用户进入最后一步后，无需标记风险为已处理也可能完成引导。
- 修复：完成按钮只有在当前风险被标记为已处理后才可使用。
- 第一轮 Chrome 验收：通过。

### 2. 重新开始没有完全恢复页面

- 现象：重新开始引导时，统计数字、详情或表格状态可能保留上一次操作结果。
- 修复：重新开始时恢复初始风险状态、筛选和详情。
- 第一轮 Chrome 验收：通过。

### 3. 侧边栏文字溢出

- 现象：较窄页面下，收起的侧边栏品牌文字可能超出范围。
- 修复：窄屏时隐藏品牌文字，只保留标记。
- 第一轮 Chrome 验收：通过。

### 4. 手机页面横向超出屏幕

- 现象：手机宽度下页面可能出现整体横向滚动。
- 修复：调整主区域宽度、间距和窄屏排列。
- 第一轮 Chrome 验收：通过。

### 5. 引导卡遮挡处理按钮

- 现象：引导到最后一步时，引导卡可能挡住“标记为已处理”按钮。
- 修复：调整桌面和手机上的引导卡位置。
- 第一轮 Chrome 验收：通过。

### 6. 风险统计口径互相矛盾

- 现象：风险总数显示 12，但当前列表只有 4 条；高风险为 3 时仍显示占比 25%；中风险没有数量；待处理和已处理数字来自另一套虚构口径。
- 原因：多个数字分别固定写在页面里，没有从同一个列表统计。
- 修复：明确四张概览卡只统计当前风险列表，并由 JavaScript 统一计算总数、风险等级、状态和高风险占比。
- 程序检查：通过。
- Chrome 回归检查：通过。

## 当前已知限制

- 数据写在本地前端文件中，刷新后恢复初始状态。
- “导出报告”“新建风险”和“关闭详情”按钮目前只是页面展示，没有实现功能。
- 页面没有登录、多用户、数据库、服务端保存或真实 SaaS 接入。
- 当前结果不能证明真实用户采用、生产稳定性或业务收益。

## 第一阶段结束条件

- 已完成最近统计修改后的 Chrome 回归检查。
- 保留最终页面、测试记录和至少一个真实 Bug 修复案例。
- 用户能够用自己的话解释：页面目的、四步路径、统计数据来源、一个 Bug 及真实性边界。
