# SaaSGuide 第二阶段数据分离记录

## 阶段目标

将引导配置和风险模拟数据从页面结构与动作程序中分离，让每类数据只有一个主要来源。

## 已完成

- `guide-data.json` 保存四个页面目标和四步引导内容。
- `risk-data.json` 保存四条模拟风险、详情、责任人、日期、状态和应对计划。
- `index.html` 只保留空的风险列表容器和详情显示位置，不再固定写入四条风险内容。
- `app.js` 同时读取两个 JSON，生成风险列表、详情、应对计划和统计数字。
- 处理风险后，当前数据状态、表格状态和概览统计同步变化。
- 重新开始引导后，风险数据恢复为 JSON 中的初始状态。

## 关键数据流

```text
guide-data.json -> app.js -> 四步引导和目标高亮
risk-data.json  -> app.js -> 风险列表、详情、计划和统计
```

## 程序检查结果

- `app.js` JavaScript 语法：通过。
- `guide-data.json` JSON 格式：通过。
- `risk-data.json` JSON 格式：通过。
- 引导步骤：4 步，顺序为 1、2、3、4。
- 风险数据：共 4 条，高风险 3 条、中风险 1 条。
- 初始状态：待处理 2 条、处理中 2 条、已处理 0 条。
- 旧的四条固定风险内容已从 `index.html` 和 `app.js` 删除，只在 `risk-data.json` 中保留。

## Chrome 回归结果

- 两个 JSON 均成功读取，页面没有停留在“加载中”：通过。
- 四条风险及对应详情和应对计划正确显示：通过。
- 全部、高风险、中风险筛选分别显示 4、3、1 条：通过。
- 四步引导顺序和目标高亮：通过。
- 处理“待处理”风险后，待处理从 2 变为 1、已处理从 0 变为 1：通过。
- 处理“处理中”风险后，待处理保持 2、已处理从 0 变为 1：通过。
- 完成和重新开始：通过，重新开始后恢复初始数据。
- 桌面布局：通过，未发现明显遮挡或页面级溢出。
- 390 × 844 手机尺寸：通过，页面无整体横向溢出，最后一步引导卡不遮挡处理按钮。
- 页面错误检查：未发现错误。

第二阶段状态：实现、程序检查和 Chrome 验收均已完成。

## 当前限制

- 两个 JSON 都是本地模拟数据，不是数据库或真实 SaaS 数据。
- 页面中的处理状态只在本次打开期间存在，刷新后恢复。
- 独立 JSON 不能通过直接双击 `index.html` 可靠读取，需要本地预览服务。
- 当前只有基础运行时错误提示；系统化字段、顺序和页面目标校验属于第三阶段。
