# SaaSGuide 当前状态

更新日期：2026-09-04。

## 命名规则

- `V1-P1` 至 `V1-P6`：现有风险看板、校验、DeepSeek、保存和收尾的历史开发阶段。
- `V2-P0`：保护并提交 V1 基线。
- `V2-P1`：统一项目数据与 XLSX 导入。
- `V2-P2`：确定性风险扫描、候选去重、固定评测和人工决策记录。
- `V2-P3`：TXT/Markdown/DOCX 证据解析、候选分类和人工核对审计。
- `V2-P4`：版本化知识文档、确定性检索、引用、冲突提示和拒答。
- `V2-P5`：SQLite 迁移、行动人工确认、状态机和事件审计。
- `V2-P6`：确定性周报指标、趋势、CSV/XLSX 导出和报告草稿。
- 单独出现的根目录 `PHASE1_TEST_RECORD.md` 至 `PHASE6_TEST_RECORD.md` 均属于 V1 历史记录。为保护引用，不改旧文件名。

## 当前 Git 状态

- `main` 的 V1 安全基线提交：`b52558e`。
- V2-P1 提交：`dd7b93e`。
- 当前开发分支：`v2-phase3-text-evidence`。
- V2-P3 增加文本证据页和解析服务，不覆盖 V1 页面或 P1/P2 数据。

## 已实现并验证

- V1 风险看板、DeepSeek ASK/PLAN、人工确认、JSON 保存和 CSV 导出保留。
- V2-P1 支持 `.xlsx` 读取、常见中英文列名建议、人工修改映射、字段与依赖校验、前 20 行预览和确认后保存。
- 原始 XLSX 副本与标准化 JSON 分开保存；JSON 保留原表行号、来源文件名和 SHA-256。
- 4 份模拟样本和固定标准答案已创建。
- 70 项自动测试通过：截至 V2-P1 的 53 项继续通过，V2-P2 新增 17 项。
- Chrome 内置样本闭环通过；有效样本保存出 6 条任务和 7 条依赖，落盘文件哈希一致。
- 390 × 844 有内容状态无页面级横向溢出；控制台没有警告或错误。
- V2-P2 固定样本生成 6 条候选、8 次规则命中；重叠规则按项目、任务和风险类型去重。
- 固定评测得到 TP=5、FP=1、FN=1，Precision=83.33%、Recall=83.33%；不是企业效果指标。
- V2-P1 最近导入的统一 JSON 可直接进入扫描；固定扫描日下生成 4 条候选。
- 确认、观察、驳回和标记误报均已在 Chrome 中写入本地审计记录，未修改 V1 风险数据。
- TXT、Markdown、DOCX 解析、来源哈希、原文位置、3/2/1 固定答案和人工核对审计已实现；真实模型提取未调用。
- 8 个自建文档版本与 10 个固定问题通过离线评测；检索、引用、拒答均为 100%，仅代表小型同源模拟集。
- SQLite 12 张业务/审计表、行动创建闸门、状态转换和逾期查询已实现；81 项累计测试通过。
- 固定周报的 6 个指标、趋势、CSV 和三表 XLSX 已实现并验收；84 项累计测试通过。

## 人工验证与自动化边界

- 用户在 Chrome 中手动确认：`valid_project_tasks_cn.xlsx` 与 `invalid_dates.xlsx` 都可通过本地文件控件选择，并可触发“生成预览”。
- 浏览器自动化无法设置该本地文件控件，因此没有自动复现真实文件选择动作。
- 文件上传端点及其解析结果由自动集成测试覆盖；页面错误与成功结果由相同 XLSX 内容的内置样本路径完成浏览器核验。
- 以上证据组合不等于真实企业文件兼容性，也不等于生产环境上传测试。

## 尚未实现

- 候选风险转为 V1 正式风险的业务联动；当前只保存人工决策审计，不自动写入正式风险。
- 向量/模型 RAG、语音、PDF/OCR、真实外部系统适配器和部署。
- Edge、Safari、macOS、Linux、多人并发、权限和生产负载验证。

进入 V2-P4 前必须以 `docs/test_records/V2_PHASE3_TEST_RECORD_2026-09-04.md` 为本阶段闸门，不得把后续路线写成已完成能力。
