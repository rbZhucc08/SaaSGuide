# Security

## 适用范围

SaaSGuide 是本地个人学习 Demo，默认只监听 `127.0.0.1`。它没有身份认证、企业权限、TLS、生产限流和恶意文件扫描，不应部署到公网，也不应接收真实客户资料或商业敏感数据。

## 密钥和本地数据

- DeepSeek 密钥只能通过进程环境变量提供；
- `.env`、日志、SQLite、模型运行记录、用户导入和备份由 `.gitignore` 排除；
- `.env.example` 只能保留占位符；
- 提交前应执行发布检查，并检查完整 Git 历史中是否曾出现密钥。

如果密钥曾经进入 Git 历史，仅删除当前文件不能使密钥失效。应立即在服务商后台撤销并更换密钥，再清理 Git 历史。

## 输入边界

上传文件、文本证据、知识文档和模型输出都按不可信输入处理：

- 上传格式和大小受限；
- 结构错误会阻断后续处理；
- 文档内文字不能覆盖程序规则；
- 模型引用必须来自当前允许的知识版本；
- 模型输出必须通过 Python 结构校验；
- 正式行动仍需用户确认。

## 报告问题

当前仓库暂未公开。如以后公开，请不要在公开 Issue 中粘贴 API 密钥、真实客户文件、运行日志或个人信息。安全问题应通过仓库所有者设置的私有联系方式报告；在联系方式建立前，建议保持仓库私有。

更完整的实现和缺口见 [docs/V2_SECURITY_AND_LIMITS.md](docs/V2_SECURITY_AND_LIMITS.md)。
