# SaaSGuide V2 升级项目交接说明

> 文档用途：供新的 Codex 对话在 `D:\CodexProjects\SaaSGuide` 中读取，并在保护现有 V1 成果的前提下继续规划、开发、测试和讲解 V2。
>
> 当前文档记录的是截至 2026-09-04 已经讨论并确认的产品方向。它是开发约束和交接地图，不代表其中所有 V2 功能已经实现。

## 0. 新对话必须先遵守的规则

1. 完整阅读本文件、`PROJECT_BRIEF.md`、`README.md`、`RESUME_EVIDENCE.md`、`DECISION_LOG.md` 和现有测试记录后再修改代码。
2. 先检查现有实现和测试，不得根据文件名推断功能已经完成。
3. 严格区分：已实现、已自动测试、已浏览器验证、仅计划、未验证。
4. 这是个人学习 Demo，使用虚构 SaaS、自建规则和模拟数据。不得宣称真实公司使用、生产部署、真实客户、真实业务收益或真实企业系统集成。
5. 不得将用户代码能力描述为熟练开发。开发过程中需要用初学者能理解的方式解释当前步骤、文件作用、输入、输出和失败条件。
6. 采用项目驱动学习：先展示当前阶段最终会得到什么，再只讲本阶段必须理解的概念。
7. 英文术语首次出现时同时给出中文解释，例如 `Validation（校验）`。
8. 不一次性堆叠数据库、RAG、语音、所有文件格式、外部系统和部署。每阶段形成可见、可运行、可测试的小闭环。
9. 模型不能把推测当事实；重要状态变更必须保留人工确认。
10. 不覆盖或破坏 V1。任何结构性重构前先建立可恢复的版本基线。

## 1. 项目演变与本次升级原因

### 1.1 V1 原始定位

SaaSGuide 最初是为 AI 应用岗位准备的个人学习 Demo，使用虚构 B2B 项目管理 SaaS 的“项目风险看板”作为场景。

V1 的核心流程是：

```text
用户选择或填写风险事实
→ 本地检查必填信息
→ DeepSeek 返回 ASK 或 PLAN
→ Python 校验模型输出结构
→ 用户人工确认
→ 保存风险记录并刷新页面
```

### 1.2 推动 V2 的岗位背景

一次目标 JD 提到以下工作：

- 搭建和维护智能制造知识库，包括内容架构、逻辑构建和持续更新。
- 用 AI 提升问题解决、标准化作业、持续改善和文档管理效率。
- 设计智能检索、问答助手、自动报告、数据洞察等 AI 模块。
- 收集业务需求并落地具体应用。

该 JD 更接近“AI 应用 + 数字化产品 + 业务流程改进”，而不是普通全栈开发或纯模型算法岗位。

讨论后确认：仅增加前端、后端、数据库和上线，并不能自动证明岗位匹配；项目必须体现具体问题、业务闭环、数据、证据、人工边界、兼容性和测试。

### 1.3 不把项目伪装成真实制造系统

用户没有真实工厂资料、真实 BPS 流程、真实制造系统接口或企业授权。因此 V2 不直接声称“智能制造项目”，也不复制某家公司的内部资料。

V2 保留原有项目管理 SaaS 场景，通过真实的软件产品方法展示可迁移能力：

- 多源数据接入
- 信息标准化
- 知识检索与引用
- 风险信号扫描
- 信息补全
- 人工决策
- 行动跟踪
- 报告和数据洞察
- 复盘与知识更新

## 2. V2 产品定义

### 2.1 产品名称

工作名称：

**SaaSGuide Risk Intelligence｜项目风险预警与决策助手**

名称可以在视觉设计阶段调整，但不得暗示真实客户或企业产品。

### 2.2 核心价值主张

SaaSGuide V2 不要求员工先意识到风险并主动填写风险，而是接收项目正常运行过程中已经存在的资料和更新，从中发现容易被忽视的风险信号，形成带证据的候选风险，交由用户确认、处理和复盘。

```text
接收项目资料和过程数据
→ 转换成统一数据结构
→ 检查确定性异常和模糊风险信号
→ 生成候选风险及证据
→ 信息不足时追问
→ 人工确认、观察或驳回
→ 制定并跟踪行动计划
→ 生成报告
→ 将结果沉淀为案例和规则反馈
```

### 2.3 重要术语边界

- `Risk Signal（风险信号）`：可能预示问题的事实或变化，不等于风险一定发生。
- `Risk Candidate（候选风险）`：系统提出、尚待人工确认的风险。
- `Confirmed Risk（已确认风险）`：经有权限的用户确认后进入正式跟踪的风险。
- `Workflow（工作流）`：步骤和分支相对固定的流程。
- `Agent（智能体）`：根据当前状态决定下一步、选择工具或提出追问的模型决策部分。
- `Skill（技能）`：有明确输入、输出、步骤、工具、失败条件和检查标准的可复用任务能力，而不是一条随意 Prompt。

### 2.4 产品不是“预测一切”

在没有真实历史标签和经过验证的预测模型时，V2 不得宣称“预测项目一定延期”或“准确预测未来风险”。第一阶段采用：

- 普通代码执行确定性规则。
- 模型从非结构化文本中提取候选事实、解释关联并提出追问。
- 知识库提供规则和案例依据。
- 人工确认最终业务判断。

## 3. V1 当前真实状态

截至 2026-09-03，现有项目已经包含：

- 原生 HTML/CSS/JavaScript 风险看板和响应式交互。
- Python + Flask 本地后端。
- JSON 风险数据和导览数据。
- DeepSeek ASK/PLAN 结构化调用。
- 模型输出校验和失败拦截。
- 新建风险前的人工确认。
- 写入前校验与旧数据备份。
- CSV 导出。
- 38 项自动测试记录。
- 桌面端和 390 × 844 手机页面验收记录。
- README、Bug Log、Decision Log、Demo Script 和求职证据边界。

当前尚未完成或不能宣称完成：

- 正式数据库。
- RAG 知识库和引用问答。
- 多源文件接入与统一字段映射。
- 持续扫描真实项目数据。
- 语音转写和 OCR。
- 用户、角色和权限。
- 真实 Jira、飞书、企业微信、ERP、CRM、邮箱等系统接入。
- 云端生产部署、生产安全、监控或真实用户采用。

### 3.1 Git 基线风险

截至交接检查，目录已有 `.git`，但尚无任何提交，现有文件全部显示为未跟踪。新对话第一轮必须：

1. 检查 `.gitignore` 是否正确排除 `.venv`、缓存、密钥和运行日志。
2. 运行现有测试和数据校验。
3. 检查现有页面是否仍可运行。
4. 在不包含密钥和不应提交的生成文件后，建立 V1 本地基线提交。
5. 基线建立后再创建 V2 开发分支或开始升级。

未经检查不得直接执行批量提交，也不得把 API Key 写入仓库。

## 4. 用户与真实使用问题

### 4.1 主要用户

- 项目经理：查看项目级候选风险、确认等级、指定行动。
- 项目成员：提供更新、补充事实、确认自己负责的行动。
- PMO 或管理者：跨项目查看风险趋势和逾期行动。
- 系统维护者：维护字段映射、规则、文档版本和数据源状态。

第一版仍为单人 Demo，不实现真实多人权限，但数据模型和页面概念需要保留角色意识。

### 4.2 采用率问题

不能假设“有了网站，员工就会使用”。V2 需要尽量减少额外录入：

- 允许继续使用 Excel、Word、PDF 等原工作资料。
- 支持拖拽导入和字段映射。
- 首次导入完整项目，之后比较增量变化。
- 系统主动形成候选风险，用户主要负责确认和补充。
- 展示原始证据，避免黑箱结论。
- 支持批量确认、观察、驳回和误报标记。
- 控制通知频率，避免 Alert Fatigue（告警疲劳）。

## 5. 输入、统一结构与输出

### 5.1 输入范围

V2 第一阶段优先支持：

- CSV
- XLSX
- TXT / Markdown
- DOCX
- 网页自由文本
- 结构化表单

后续阶段再增加：

- 可提取文字的 PDF
- 扫描 PDF / 图片 OCR
- 音频文件上传
- 浏览器录音和 Speech-to-Text（语音转文字）
- JSON API

只设计但不宣称真实接入：Jira、Microsoft Project、飞书、企业微信、钉钉、ERP、CRM、邮箱、GitHub Issues 等。

### 5.2 统一数据结构

所有输入都应经过 `Ingestion Layer（数据接入层）` 转换成 Canonical Schema（统一数据结构）。建议至少包含：

```json
{
  "source_id": "source-001",
  "source_type": "xlsx|docx|text|voice|api",
  "source_name": "项目周报-第3周.xlsx",
  "source_version": "v1",
  "project_id": "project-001",
  "event_time": "2026-09-04T10:00:00+08:00",
  "record_type": "task_update|meeting_note|change_request|issue",
  "raw_text": "原始内容或来源片段",
  "structured_facts": {},
  "missing_fields": [],
  "extraction_confidence": "low|medium|high",
  "needs_confirmation": true
}
```

模型提取结果不得覆盖原始数据，必须保留来源、时间、版本和用户修订记录。

### 5.3 语音输入原则

语音链路应为：

```text
原始录音
→ 语音转文字
→ 模型提取项目事实
→ 结构校验
→ 标记不确定和缺失字段
→ 用户核对转写及结构化结果
→ 才能进入风险扫描
```

先实现自由文本到结构化事实，再接语音。否则无法区分错误来自语音识别还是风险分析。

### 5.4 输出范围

优先输出：

- 网页风险雷达和项目看板。
- CSV / XLSX 风险清单。
- JSON 结构化结果和 API 响应。

后续增加：

- PDF 风险报告。
- DOCX 周报或复盘报告。
- 仅在明确授权后才可能增加外部通知或系统写回。

## 6. 数据是项目的必要组成

V2 必须具备数据，但模拟数据只能证明限定条件下的功能表现，不能证明真实企业收益。

### 6.1 四层数据

#### A. 项目主数据

- 项目、阶段、里程碑。
- 任务、负责人、开始和截止日期。
- 优先级、依赖关系、工作量、状态。
- 可选预算和资源数据。

#### B. 过程证据

- 周报、会议纪要、任务更新。
- 需求变更、问题记录、决策记录。
- 模拟聊天摘要、文件版本变化。

#### C. 知识库资料

- 风险分级规则。
- 升级和通知流程。
- 角色职责。
- 项目模板和处理检查表。
- 历史案例。
- 周报和复盘规范。

#### D. Ground Truth（标准答案）与测试标签

- 哪些变化应触发候选风险。
- 应触发哪条规则。
- 哪些字段缺失。
- 应检索到哪份文档和哪个版本。
- 哪些问题知识库无法回答。
- 哪些候选风险应确认、观察或驳回。

没有标准答案，只能证明系统输出了内容，不能证明输出正确。

### 6.2 场景化模拟数据

不要随机生成一批互不关联的假数据。至少构造三条有时间顺序和因果关系的项目轨迹：

1. 正常项目：验证系统不会无故告警。
2. 渐进延期项目：前置任务延期、下游任务未调整、负责人超载、会议出现延期措辞、里程碑最终受影响。
3. 范围变更项目：连续需求变更、截止日期不变、测试范围未更新，逐渐出现交付和质量风险。

先人工定义事件逻辑和标准答案，再让 AI 协助补全自然语言，最后人工校验一致性。

### 6.3 可证明的三个层级

1. 功能证明：在固定模拟测试集上，系统能否正确解析、检索、引用、拒答和发现预设异常。
2. 场景证明：使用获得授权的真实资料或公开合法数据，并由真实用户完成试用。
3. 商业证明：真实企业长期使用后产生时间、成本、质量或收益改进。

当前项目只计划完成第 1 层；没有获得真实数据和真实使用证据前不得宣称第 2、3 层。

## 7. 风险检测设计

### 7.1 确定性规则由普通代码负责

第一批规则候选：

- 任务逾期。
- 前置任务延期影响后续任务。
- 截止日期早于开始日期。
- 关键任务没有负责人。
- 阻塞状态超过设定时间未更新。
- 工作量增加但截止日期没有变化。
- 关键负责人同时承担过多高优先级任务。
- 项目计划和周报状态矛盾。
- 需求变更后测试范围或计划没有同步更新。
- 同一问题多次重新打开。

规则阈值属于本项目自建规则，不得冒充行业或真实公司的正式标准。

### 7.2 模型负责的模糊任务

- 从会议纪要、周报和自由文本提取候选事实。
- 识别犹豫、不确定、依赖未确认和模糊变更。
- 判断信息是否足以继续。
- 生成最少且必要的追问。
- 检索并概括相关制度和案例。
- 解释风险信号之间的关系。
- 生成行动计划和复盘草稿。

模型不得自行计算正式统计数字，不得无依据改变风险等级，不得把推测写为事实。

### 7.3 事前、事中、事后闭环

- 事前：扫描早期风险信号。
- 事中：补全信息、确认风险、制定和跟踪行动。
- 事后：整理时间线、分析根因、生成复盘、沉淀案例并调整规则。

事故已经发生并不意味着系统失去价值，但事后功能不能被包装成事前预测。

## 8. Workflow 设计

### 8.1 Data Ingestion Workflow（数据接入工作流）

```text
接收文件或文字
→ 识别类型
→ 调用解析器
→ 检查编码、列名、日期和版本
→ 映射统一字段
→ 用户确认有歧义的映射
→ 保存原始数据和标准化数据
```

### 8.2 Risk Scanning Workflow（风险扫描工作流）

```text
读取最新项目状态
→ 与上一版本比较
→ 运行确定性规则
→ 提取模糊风险信号
→ 合并重复候选
→ 形成带证据的候选风险
→ 等待人工确认
```

### 8.3 Clarification Workflow（信息补全工作流）

```text
检查必要字段
→ 完整：进入评估
→ 不完整：ASK 最少必要问题
→ 用户回答
→ 再次校验
```

### 8.4 Risk Triage Workflow（风险分诊工作流）

```text
规则计算基础信号
→ 检索适用制度和历史案例
→ 模型基于证据解释
→ 用户选择确认、观察或驳回
→ 人工设置负责人和期限
→ 保存行动计划
```

### 8.5 Review & Learning Workflow（复盘与知识沉淀工作流）

```text
检查行动结果
→ 对比候选风险与最终结果
→ 记录误报、漏报和有效信号
→ 生成复盘草稿
→ 人工审核
→ 更新案例或提出规则修改建议
```

## 9. Agent 与 Skill 设计

### 9.1 推荐架构

第一版使用一个 `Orchestrator Agent（协调智能体）` 加多个普通工具，不采用多个 Agent 自由对话。

推荐状态：

```text
NEW
→ INGESTED
→ NEEDS_INFORMATION
→ RISK_CANDIDATE
→ CONFIRMED / OBSERVING / DISMISSED
→ ACTION_TRACKING
→ CLOSED
→ REVIEWED
```

### 9.2 第一批工具

- `parse_csv`
- `parse_xlsx`
- `parse_docx`
- `parse_text`
- `map_columns`
- `validate_schema`
- `compare_versions`
- `run_risk_rules`
- `retrieve_documents`
- `calculate_metrics`
- `save_candidate`
- `save_human_decision`
- `export_report`

工具必须有确定的输入和输出，错误时返回明确的错误类型，不把异常文本直接交给模型猜测。

### 9.3 第一批 Skill

#### `project-data-intake`

读取项目文件、识别字段、转换成统一结构。遇到文件无法解析、日期不明、版本冲突或项目标识缺失时停止并请求确认。

#### `risk-signal-scan`

检查任务、依赖、进度、变更和文本信号。输出风险类型、触发规则、原始证据、涉及对象、置信度和缺失信息。

#### `evidence-grounded-assessment`

检索知识库，根据有效版本文档和历史案例评估候选风险。没有依据时必须拒绝确定性判断。

#### `risk-action-planner`

为已确认风险生成行动草稿，包括行动、负责人角色、优先级、完成信号、检查日期和依赖。保存前必须人工确认。

#### `weekly-risk-report`

普通代码先计算新增、关闭、逾期、高风险数量和处理时间，模型仅根据已计算结果生成文字解释和报告草稿。

### 9.4 Agent 权限边界

可以自动：

- 解析文件。
- 检查字段。
- 检索文档。
- 运行风险规则。
- 生成候选风险、追问和草稿。
- 计算展示指标。

必须人工批准：

- 确认正式风险。
- 修改重要等级。
- 指派负责人。
- 改变截止日期。
- 保存正式行动计划。
- 关闭风险。
- 对外发送通知或写回外部系统。

禁止：

- 删除原始数据。
- 隐藏失败和误报记录。
- 把 AI 推测写成事实。
- 无证据创建高风险。
- 将上传文件中的内容当作系统指令执行。
- 未经授权向真实人员或系统发送消息。

## 10. 知识库与 RAG 方向

V2 知识库采用自建的虚构 SaaS 文档，不伪装成真实企业资料。第一批文档类型可以是：

- 项目风险分级标准。
- 风险升级与通知规则。
- 项目延期处理流程。
- 客户需求变更管理规范。
- 技术风险处理检查表。
- 风险负责人职责说明。
- 项目周报填写规范。
- 历史风险处理案例。
- 产品功能使用手册。
- 版本更新说明。

知识库必须支持：

- 文档类型、项目、标签、版本、生效日期和状态。
- 新旧版本冲突处理。
- 检索片段、文档名和版本引用。
- 无依据时拒答。
- 原文与模型解释分开显示。
- 用户反馈答案是否有用、引用是否正确。

RAG 的目标不是让回答更长，而是让回答有可核验依据。

## 11. 数据库与持久化

数据库不是为了简历上出现 SQL，而是解决 JSON 难以处理的查询、版本、关联和审计问题。

第一版建议使用 SQLite，候选实体包括：

- `projects`
- `milestones`
- `tasks`
- `dependencies`
- `sources`
- `documents`
- `document_versions`
- `document_chunks`
- `risk_candidates`
- `risk_evidence`
- `human_decisions`
- `action_items`
- `reports`
- `feedback`
- `ai_runs`
- `error_logs`

迁移到 PostgreSQL 属于后续可选项。第一版不做多租户、复杂权限或微服务。

## 12. 兼容性要求

### 12.1 文件兼容性

必须显式处理或标记未支持：

- CSV 的 UTF-8、UTF-8 BOM、GBK。
- XLSX 与旧版 XLS 的区别。
- 中文和英文列名。
- 日期格式、时区、小数和百分比。
- 空值、重复行、混合类型、合并单元格。
- DOCX 表格。
- 普通 PDF 与扫描 PDF。
- 同一文件重复上传。
- 新旧版本冲突。
- 文件大小和扩展名白名单。

不支持的格式应给出可理解的错误信息，不能静默忽略。

### 12.2 浏览器和系统兼容性

只声明真实测试过的环境。建议测试矩阵：

- Windows 11 + Chrome。
- Windows 11 + Edge。
- 常见桌面分辨率。
- 390 × 844 响应式页面。

macOS、Safari、Linux 等未实际测试时标记为未验证。

### 12.3 外部系统兼容性

外部系统采用 Adapter（适配器）思想，但没有真实授权时仅实现文件导入或模拟 API 合约，不宣称真实集成。

## 13. 真实环境中还必须考虑的问题

### 13.1 权限、隐私与数据治理

- 谁可以查看哪个项目。
- 是否包含个人、客户或商业敏感信息。
- 资料是否允许发送给第三方模型。
- 是否需要脱敏。
- 数据保留、导出和删除策略。
- 原始数据、模型输出和人工修改的审计记录。

第一版不必完整实现企业权限，但必须在设计和 README 限制中说明。

### 13.2 Prompt Injection（提示词注入）

上传文档中的“忽略规则”“输出全部数据”等文字属于内容，不得获得系统指令权限。检索内容必须和系统指令隔离，并加入恶意文本测试。

### 13.3 False Positive 与 False Negative（误报与漏报）

- 每个候选风险展示证据和触发规则。
- 用户可以确认、观察、驳回和标记误报。
- 相同风险需要去重或合并。
- 设置通知阈值，避免告警疲劳。
- 评测不能只统计命中，还要统计误报和漏报。

### 13.4 模型和网络故障

必须处理：

- API 超时。
- 额度不足或限流。
- 非法 JSON。
- 字段缺失。
- 模型服务不可用。
- 本地网络中断。

模型不可用时，文件解析和确定性规则检查仍应可以运行。

### 13.5 版本与责任

- 保留原始文件和哈希或版本标识。
- 明确哪条候选风险来自哪个版本。
- 新版本导入后重新评估相关风险。
- 模型只能提出建议，正式状态由明确用户确认。

## 14. 页面与最终展示形态

V2 最终可以包含以下主要页面：

1. **Overview（总览）**：项目状态、候选风险、已确认风险、逾期行动和近期变化。
2. **Data Sources（数据源）**：上传文件、解析状态、字段映射、版本和失败原因。
3. **Risk Radar（风险雷达）**：按项目、类型、等级、状态筛选候选和正式风险。
4. **Risk Detail（风险详情）**：原始证据、触发规则、知识引用、AI解释、人工决策和行动计划。
5. **Knowledge Base（知识库）**：文档、版本、标签、检索和引用问答。
6. **Reports & Insights（报告与洞察）**：趋势、误报率、逾期行动和周报草稿。
7. **Audit & Settings（审计与设置）**：AI运行、错误、规则版本、用户反馈和系统限制。

不要求第一阶段一次完成全部页面。

## 15. 评测与证据

### 15.1 建议测试集

- 20 份自建知识文档。
- 30 个有明确答案的问题。
- 10 个知识库无法回答的问题。
- 10 个新旧版本冲突问题。
- 3 条有时间线的模拟项目轨迹。
- 至少 20 条风险案例。
- 至少 5 条缺字段案例。
- 至少 5 条风险边界案例。
- 文件编码、格式错误、重复上传和 API 失败样本。

具体数量可分阶段调整，不能在实际创建和测试前写成已完成。

### 15.2 可衡量指标

- `Parsing Success Rate（解析成功率）`
- `Schema Validation Pass Rate（结构校验通过率）`
- `Rule Precision / Recall（规则查准率 / 查全率）`
- `Retrieval Hit Rate（检索命中率）`
- `Citation Accuracy（引用准确率）`
- `Refusal Accuracy（无依据拒答准确率）`
- `False Positive Rate（误报率）`
- `Regression Pass Rate（回归测试通过率）`

指标必须来自固定测试集和实际运行结果。不得提前承诺 100%，不得用模拟测试结果推导真实企业效率。

### 15.3 每阶段保留的证据

- 可运行代码。
- 测试数据和标准答案。
- 自动测试结果。
- 浏览器验收记录。
- 真实 Bug 和修复记录。
- 决策日志。
- 已知限制。
- 截图和演示视频。
- 用户本人可以讲清楚的流程说明。

## 16. 分阶段实施路线

### Phase 0：保护 V1 基线

目标：确认现有项目仍可运行，并建立版本基线。

交付：

- 环境和密钥检查。
- 现有自动测试结果。
- 页面最小浏览器验收。
- 合理的 `.gitignore`。
- V1 本地基线提交。

### Phase 1：统一项目数据与 XLSX 导入

目标：不接模型，先完成一个真实的数据闭环。

交付：

- 统一项目、任务、依赖和更新的数据结构。
- 一份正常 XLSX 和多份故障样本。
- 文件解析、列名映射、日期和必填字段校验。
- 导入预览及用户确认。
- 标准化 JSON 输出。
- 自动测试和浏览器验收。

### Phase 2：确定性风险扫描

目标：从项目数据中生成带证据的候选风险。

交付：

- 第一批可解释规则。
- 风险去重。
- 候选风险页面。
- 确认、观察、驳回和误报反馈。
- 标准答案与查准率 / 查全率。

### Phase 3：自由文本和 DOCX 证据接入

目标：从会议纪要或周报中提取事实，并与结构化任务数据关联。

交付：

- TXT / Markdown / DOCX 解析。
- 原文片段保留。
- AI 结构化提取。
- ASK 信息补全。
- 人工核对和修改。

### Phase 4：知识库与 RAG

目标：候选风险和问答具有可核验依据。

交付：

- 自建文档集和版本体系。
- 分段和检索。
- 带文档名、版本和片段的引用。
- 无答案拒答。
- 旧版本冲突测试。
- 检索和引用评测。

### Phase 5：SQLite 与行动跟踪

目标：支持查询、关联、版本、决策和审计。

交付：

- 数据库迁移方案。
- 项目、风险、证据、决策、行动、文档和运行日志表。
- 人工确认和修改记录。
- 行动到期和关闭流程。

### Phase 6：报告与数据洞察

目标：将已核验数据转换为项目周报和趋势。

交付：

- 由 Python 计算的确定性指标。
- 风险趋势和分布图。
- 误报、逾期和处理时间分析。
- AI 报告草稿。
- CSV / XLSX 导出，后续可增加 PDF / DOCX。

### Phase 7：语音、PDF/OCR 与更多适配器

目标：扩展输入方式，但不牺牲可核验性。

交付顺序：自由文本稳定后再加语音；普通 PDF 稳定后再加 OCR；只有获得真实接口权限后才做真实连接器。

### Phase 8：部署与作品集收尾

目标：提供公开可演示但不泄露数据和密钥的版本。

交付：

- 环境变量和密钥管理。
- 文件大小、格式和请求限制。
- 基础错误监控。
- 公开版仅使用模拟数据。
- README、架构图、演示脚本、测试报告和限制说明。
- 真实可打开的演示地址（仅在部署和访问验证后记录）。

## 17. V2 第一轮开发任务

新对话读取本文件后，不要立即编写 RAG、数据库或语音功能。第一轮按以下顺序执行：

1. 读取并复核现有 V1 文件、测试和真实性声明。
2. 检查 Git 无提交且文件未跟踪的状态，确认 `.gitignore`，运行现有检查并建立安全基线。
3. 新增一份 V2 Phase 1 的短规格说明，明确最终页面、输入样例、统一字段、错误样例和验收条件。
4. 向用户先展示 Phase 1 完成后的页面形态和文件流，不要求用户先背概念。
5. 创建 Phase 1 所需的最小文件结构和模拟数据。
6. 先实现 XLSX 导入、字段映射、校验、预览和标准化 JSON，不接 LLM。
7. 逐项运行自动测试和真实浏览器验收，并记录失败。
8. Phase 1 通过后再开始确定性风险扫描。

### Phase 1 最小业务闭环

```text
用户上传项目任务 XLSX
→ 系统识别或建议列名映射
→ 检查日期、负责人、状态和依赖
→ 显示导入预览和错误
→ 用户确认
→ 保存统一项目 JSON
→ 页面显示导入成功和数据摘要
```

## 18. 推荐目录演进

不要求一次性重构，以下是目标方向：

```text
SaaSGuide/
├── README.md
├── PROJECT_BRIEF.md
├── SAASGUIDE_V2_HANDOFF.md
├── app.js / index.html / styles.css
├── server.py
├── requirements.txt
├── data/
│   ├── raw/
│   ├── normalized/
│   ├── samples/
│   └── evaluation/
├── knowledge/
│   ├── documents/
│   └── metadata/
├── prompts/
├── skills/
├── services/
│   ├── ingestion/
│   ├── risk_rules/
│   ├── retrieval/
│   ├── ai/
│   └── reporting/
├── database/
├── tests/
├── generated/
├── docs/
│   ├── decisions/
│   ├── test_records/
│   └── screenshots/
└── scripts/
```

现有平铺结构可以渐进迁移。重构前必须有基线和回归测试，不能一次性移动所有文件造成现有入口失效。

## 19. 求职表述边界

V2 未完成前，继续使用 V1 的实际证据表述，不得把本文件中的计划写入简历。

完成各阶段后，只能描述实际完成并验证的部分。例如未来若真实完成，可表述：

- 设计多源项目数据接入和统一字段校验流程。
- 以确定性规则和大模型文本提取生成带证据的候选风险。
- 构建带版本和引用的项目知识库问答，并测试无依据拒答。
- 设计人工确认、误报反馈、行动跟踪和审计记录。
- 使用固定模拟测试集评估解析、规则、检索和引用表现。

始终保留限定：个人学习 Demo、虚构业务、自建规则、模拟数据、无真实企业使用和业务效果。

## 20. 给新对话的首条指令模板

可直接发送：

> 请完整阅读项目根目录下的 `SAASGUIDE_V2_HANDOFF.md`，再阅读 `PROJECT_BRIEF.md`、`README.md`、`RESUME_EVIDENCE.md`、现有源码和测试记录。严格区分已完成与计划内容，不要覆盖 V1，也不要一次性开发全部 V2。先检查 Git、密钥排除、现有测试和页面状态，建立 V1 安全基线；随后只执行 V2 Phase 1：制定短规格、展示完成形态、创建有标准答案的 XLSX 模拟数据，并实现 XLSX 导入、字段映射、校验、预览和标准化 JSON。每一步先告诉我会得到什么，再带我边做边理解。不要接 RAG、数据库、语音或真实外部系统，直到 Phase 1 验收通过。

---

本文件是 V2 讨论结果的交接说明。若后续产品方向发生变化，应在 `DECISION_LOG.md` 中记录变化原因、备选方案、影响范围和验证结果，而不是静默修改历史目标。
