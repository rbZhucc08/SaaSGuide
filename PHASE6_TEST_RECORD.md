# 第六阶段异常与回归测试记录

## 目的

验证正确输入可以继续流转，错误输入和保存故障能够在写坏数据前停止。

## 覆盖范围

- JSON 格式、缺字段、重复步骤、重复风险编号、非法状态和不存在的页面目标。
- DeepSeek 密钥缺失、返回非法 JSON、非法优先级、行动步骤跳号和空完成信号。
- Flask 非 JSON 请求、请求体超过 64 KB、上下文过长和缺少 risk 对象。
- ASK 不生成 PLAN 文件，也不能保存为风险。
- 输出路径不可写、风险数据文件缺失和非法截止日期。
- 合法低风险 PLAN 能正确映射、保存并生成备份。
- 原风险看板、引导生成器和新建风险功能的回归检查。

## 结果

- 2026-09-03 执行 38 项自动测试，全部通过。
- `app.js`、`builder.js` JavaScript 语法检查通过。
- `guide-data.json`、`risk-data.json` 与页面目标校验通过。
- 项目中未发现硬编码 DeepSeek 密钥。
- 桌面与 390 × 844 手机页面检查通过。

## 真实错误样本

新建风险第一次调用 DeepSeek 时，服务无法访问外部网络。页面明确显示失败且没有增加风险；重新启动可联网服务后，同一输入分析成功。该过程验证了失败不会被误报为成功，也不会越过人工确认直接写入数据。

## 边界

这些测试证明已列场景的行为，不等于生产级安全审计、压力测试或长期稳定性保证。并发写入、恶意攻击、断电恢复和真实多用户冲突未验证。

