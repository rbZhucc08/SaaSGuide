# SaaSGuide AI 风险助手调整与测试记录

## 为什么调整

原来的 AI 引导生成器主要服务产品运营或配置人员，却被放在员工使用的风险看板主入口，和“降低内部员工管理项目风险难度”的目标不一致。固定四步引导也不需要每次调用模型。

2026-09-03 将产品主线调整为：员工直接打开风险看板，选中具体风险后按需使用 DeepSeek 分析；原引导生成器保留为后台学习实验，不再出现在主页面入口。

## 当前用户流程

1. 用户打开风险看板，直接查看当前风险和统计。
2. 用户选择一条风险，可补充最新情况。
3. 点击“AI 分析当前风险”。
4. 信息不足时返回 ASK；信息充分时返回 PLAN。
5. PLAN 显示建议等级、优先级、判断依据、行动步骤和完成信号。
6. AI 不会自动修改风险等级或状态。
7. 只有用户点击“采纳为当前应对计划”后，页面中的应对计划才更新；刷新后恢复模拟数据。

## DeepSeek 的必要作用

本地程序负责固定字段、格式和安全规则；DeepSeek 负责理解不同风险描述的语义、判断信息是否足够，并把分散信息整理成可执行建议。它不再用于重复生成固定教程。

## 代码与数据流

- `index.html`：风险详情内显示 AI 风险助手。
- `app.js`：提交当前选中风险、展示 ASK / PLAN、处理用户采纳动作。
- `deepseek_risk_assistant.py`：构造 DeepSeek 请求并验证结构化结果。
- `server.py`：提供 `/api/risks/analyze`，保存合格 PLAN 和运行日志。
- `generated/latest-risk-analysis.json`：最近一次合格风险分析。
- `generated/risk-assistant-run-log.jsonl`：分析运行记录，不保存 API 密钥。

## 自动检查

执行命令：

```powershell
& '.\.venv\Scripts\python.exe' -W error::ResourceWarning -m unittest -v test_validator.py test_deepseek_ask_build.py test_risk_assistant.py test_server.py
```

结果：31 项测试全部通过。

新增覆盖：

- 风险缺字段时本地返回 ASK，不调用 API。
- 合法 PLAN 可以进入页面流程。
- 模型 ASK 可以被解析。
- 非法优先级、跳号步骤和空完成信号会被拦截。
- Flask 只为 PLAN 保存分析文件，ASK 不生成计划文件。
- 风险对象缺失和过长补充说明会被拒绝。

Python、JavaScript 语法和原风险数据校验均通过。

## 真实浏览器与 API 验收

测试风险：`第三方 API 联调延迟`，虚构项目数据。

- DeepSeek 模型：`deepseek-v4-flash`。
- 真实返回：PLAN。
- 使用量：1267 tokens。
- 建议等级：高风险。
- 建议优先级：立即处理。
- 生成 3 条行动建议，每条都有完成信号。
- 页面控制台无错误或警告。
- 点击“采纳”后，应对计划更新为 3 条 AI 建议。
- 采纳前后风险等级保持“高风险”，状态保持“待处理”，风险总数保持 4，已处理保持 0。
- 刷新页面后恢复原始模拟应对计划，说明 AI 没有偷偷改写源数据。

## 导览调整

四步导览没有删除，但改为默认不弹出。用户打开页面后可以直接管理风险；需要帮助时再点击右下角“开始导览”。实测启动后步骤 1 和高亮正常，退出后高亮清除。

## 响应式检查

- 桌面：AI 风险助手位于所选风险详情内。
- 390 × 844：页面宽度和视口均为 390，无页面级横向溢出。
- 手机端 AI 助手宽约 321 像素，分析按钮宽约 289 像素；可选导览提示宽 310 像素。

## 当前限制

- 这是虚构 SaaS、模拟风险和个人学习 Demo，不代表真实公司项目。
- DeepSeek 输出是建议，不是经过项目现场验证的事实或专业风险结论。
- 目前只能分析列表中已有风险，“新建风险”仍未实现。
- 采纳建议只影响当前浏览器页面，刷新后恢复；没有数据库或长期历史版本。
- 真实 API 已验证 PLAN；风险助手的 ASK 分支目前通过离线测试，尚未用主页面真实触发。
- 没有并发、权限、敏感数据脱敏或生产部署设计。
